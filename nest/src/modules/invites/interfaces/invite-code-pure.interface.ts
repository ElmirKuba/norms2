/**
 * InviteCodePure — содержательное поле кода приглашения (ADR-0033). Нижний слой.
 */
export interface InviteCodePure {
  /** Причина приглашения (предупреждение про ПДн в UI). */
  reason: string;
}
