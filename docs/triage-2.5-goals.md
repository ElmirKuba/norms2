# Триаж 2.5·23 — «Цели работают не до конца» (полный анализ + фикс всех багов)

> **Зачем этот файл.** Elmir потыкал цели в браузере на localhost (создал «Тест отжиманий»,
> добавил вехи, записал замер) — **ничего не поменялось**. Решено не делать поверхностный
> багресёрч, а полный анализ кодовой базы фичи «Цели» (фича-в-фиче Акцента). Это **резюмируемый
> реестр находок** (выжить между лимитами): анализ-подпункты `·23.A*` находят баги и пишут сюда;
> из находок рождаются фикс-подпункты `·23.F*`; в конце — свод и проверка. Трекер шагов —
> [`impl-phase2-plan.md`](./impl-phase2-plan.md) (раздел 2.5·23).
>
> **Статусы:** 🔴 блокер · 🟠 баг · 🟡 мелочь/полировка · ✅ исправлено · 🔎 под анализом.

---

## Реестр находок

### F#1 — 🔴→✅ Формы «Записать прогресс» и «Добавить веху» не отправляют запрос
- **Симптом (Elmir):** на детали цели вводишь замер/значение или вводишь веху → жмёшь кнопку →
  ничего не происходит, в БД пусто.
- **Доказательство:** в логах nest за сессию — **0 POST** на `/goals/:id/entries` для его целей и
  **0 POST** на `/goals/:id/milestones` вообще (таблица `milestones` пуста во всей БД; `goal_entries`
  для «Тест отжиманий» и «1000 отжиманий» пусты). Браузер слал только GET'ы. Бэк исправен (curl-смоук
  patch 9 писал записи 201).
- **Корень:** в `goal-detail.component.ts` обе формы — `<form (ngSubmit)="…">` **без `[formGroup]`**
  и без `FormsModule` (импортирован только `ReactiveFormsModule`, поля — одиночные `[formControl]`).
  `(ngSubmit)` эмитит **директива формы** (`FormGroupDirective` по `[formGroup]` либо `NgForm` из
  `FormsModule`). Ни одной из них на форме нет → `ngSubmit` **не срабатывает**, `record()`/
  `addMilestone()` не вызываются, POST не уходит. Компилируется без ошибок, в рантайме мёртвая.
  Латентно: `Enter` в одиночном поле мог бы вызвать нативный submit (перезагрузку).
- **Почему не поймали:** «live-verify» всего 2.5 был **curl/API-смоуками**, не кликами в браузере
  (память `tests-deferred`). Прод-сборка лишь проверяет компиляцию. Elmir — первый реальный клик.
- **Охват:** только `goal-detail` (2 формы). Остальное в этом компоненте — правка/удаление записи,
  lifecycle-меню, adopt, удаление вехи — на `(click)`, **работает**. Другие accent-формы
  (логин/регистрация/модалки привычек/микро-побед/целей) используют `[formGroup]` — **не задеты**.
- **Фикс:** обе формы обёрнуты в реальный `FormGroup` + `[formGroup]` + `formControlName`
  (как в логине/модалках). Теперь `ngSubmit` эмитится (клик и Enter), нативной перезагрузки нет.
  `recordForm {value}` и `msForm {title, thresholdValue}`; `editControl` (правка записи) оставлен
  одиночным — он на `(click)`, корректен.
- **Проверка:** prod-build зелёный; рантайм-гарантия — `[formGroup]` всегда даёт `ngSubmit`
  (тот же паттерн, что в работающем логине). **Нужен браузер-смоук Elmir'а** (запись/веха →
  процент/история/вехи меняются; цифры в БД).

---

## План анализа (анализ-подпункты ·23.A*) — что ещё пройти

> F#1 закрывал главный симптом, но Elmir просил **полный** анализ. Ниже — план прохода; каждый
> подпункт по завершении дописывает находки в «Реестр» выше.

- [ ] **·23.A1 — фронт goal-detail (рантайм-поведение).** Помимо F#1: проверить swallowed-ошибки
      (`error: () => undefined` глушит 4xx/5xx молча — UX-риск), обновление сигналов после записи,
      пагинацию истории, авто-complete из ответа, отображение forecast/прогноза.
- [ ] **·23.A2 — фронт goals-list + dashboard + форма создания/правки.** Сабмиты (формы в модалках
      на `[formGroup]` — ок), фильтры, стартер-пак CTA, бейджи, навигация.
- [ ] **·23.A3 — бэк: вычисление прогресса (ADR-0052).** Все 3 рода, edge (нет записей, target=start,
      reduce<start, паузы вне activeDays, TZ-граница дня), rollup смешанных единиц, авто-complete.
- [ ] **·23.A4 — бэк: записи/вехи/lifecycle/привычка→цель.** Идемпотентность, каскады, права доступа
      (чужая цель), валидации DTO, пороги вех.
- [ ] **·23.A5 — границы слоёв (grep-аудит).** ORM/инфра не течёт в домен (ADR-0030/0034/0050).
- [ ] **·23.A6 — свод + приоритизация + фикс-подпункты ·23.F*.** Свести всё, завести фиксы, выполнить,
      перепроверить. Только после зелёного — выход из ·23 к ·24.

---

## Журнал
- 2026-06-24: заведён файл; F#1 найден (root cause: `ngSubmit` без `[formGroup]`) и исправлен;
  составлен план ·23.A1…A6. Дальше — продолжать по плану, дописывая находки.
