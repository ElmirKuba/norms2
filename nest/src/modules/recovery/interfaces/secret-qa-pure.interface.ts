/**
 * SecretQaPure — содержательное поле секретного вопроса (ADR-0033), без id/FK/
 * хеша/системных меток. Нижний слой иерархии типов.
 */
export interface SecretQaPure {
  /** Текст вопроса (из безопасного списка или свой, ADR-0008). */
  question: string;
}
