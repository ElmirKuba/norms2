# impl-phase1-plan.md — план реализации Фазы 1 (resumable)

> Пошаговый план кодинга ЛК. Лимиты ~5ч непредсказуемы → дробим на мелкие этапы, каждый = рабочая точка + коммит. На новой сессии: открыть этот файл → «Текущая позиция» → продолжить. Дока-источник: `docs/` (database, domain-model, api-contracts, backend, architecture, getting-started) + ADR.
>
> Стек: NestJS (5-слойка: controllers→use-cases→domain-services→adapters→repositories) + Drizzle + Postgres, npm, Docker. Angular standalone+Signals, SCSS, MatDialog. id `uuidv7___unixmillis`.

## Текущая позиция (по факту)
- **Окружение:** Node 24, npm 11, Docker 29, Angular CLI глобально, nest — через npx.
- **`nest/` B2+B3 ГОТОВ:** NestJS 11. Конфиг zod fail-fast (`system/config`), логи pino (`system/logging`), Drizzle (`database/client`: `DatabaseService`+токен `DRIZZLE`, схемы `database/schemas`; `db:generate/migrate/studio`), health (`modules/health`), exception-filter (`shared/filters`). **Раскладка — feature-first + вынесенный `database/`** ([ADR-0034](./decisions/0034-feature-first-layout.md)). Сборка `tsc` зелёная. ⚠️ **Линт nest красный** — почистить под строгий eslint отдельно (Sonnet).
- **`angular/` СОЗДАН:** Angular 21, standalone + Signals, SCSS, npm. Строгий eslint согласован с nest. Оболочка `App` с `<router-outlet/>`; `index.html` — `lang="ru"`, title «Нормисы». `build`+`lint`+`test` зелёные.
- **Docker dev:** `make dev-up` → postgres + pgadmin + **nest (watch, volume на src)**; пути через `${PROJECT_ROOT}`. Hot-reload проверен.
- **Следующий: S1** (первые таблицы Drizzle в `database/schemas`: accounts, secret_qa, invite_codes, invitations, bans, sessions; тогда же — ADR по concurrency/`version`).
- **Версии:** всё — latest stable на сегодня ([ADR-0021](./decisions/0021-tooling-defaults.md)).

## Этапы (каждый — отдельный коммит)

### B. Bootstrap
- [x] **B1** — `nest/` каркас ✅ (NestJS 11, npm, строгие tsconfig+eslint, JSDoc на русском, сборка зелёная) + `angular/` каркас ✅ (Angular 21, standalone+Signals, SCSS, строгий eslint согласован с nest, оболочка с router-outlet, build/lint/test зелёные).
- [x] **B2** — ✅ docker `compose-files/docker-compose.dev.yml` (postgres+pgadmin+nest watch с volume на src), `.env.example`, Drizzle + drizzle-kit в nest (`system/database`, токен `DRIZZLE`, `db:generate/migrate/studio`), конфиг zod fail-fast (`system/config`), Makefile `db-*`. Проверено вживую: бэк стартует (хост+контейнер), пинг БД, fail-fast, hot-reload. ⚠️ Линт nest пока красный — почистить отдельно (Sonnet).
- [x] **B3** — ✅ health `GET /api/v1/health` (liveness) + `/health/ready` (readiness, пинг БД→503 если недоступна), глобальный префикс `/api/v1`, CORS на dev-фронт, глобальный exception-filter (конверт `{error:{code,message,details?}}`, 5xx без стектрейса наружу), pino (`nestjs-pino`: request-id, redact секретов, pretty в dev, health вне autoLogging). 5-слойка: `controllers/health`→`use-cases/health`→`interfaces/health`, фильтр в `filters/`, логи в `system/logging`. Проверено вживую: health/ready→200, 404→конверт, pino-логи.
- [x] **B4** — ✅ util `generateId()` (uuidv7___unixmillis, ADR-0016) — параллельные копии без зависимостей: `nest/src/shared/utility-level/generate-id.util.ts` и `angular/src/app/core/utils/generate-id.util.ts` (не шарим). Каркас 5-слойки — сделан при feature-first переезде (`system/`, `modules/`, `database/`, `shared/`).

### S. Схема БД (Drizzle)
- [x] **S1** — ✅ S1.1 concurrency ([ADR-0035](./decisions/0035-concurrency-control.md)); S1.2 Drizzle-схемы 6 таблиц в `database/schemas/` (accounts с `version`, CHECK `registration_source`, `UNIQUE(lower(login))`; partial-unique на bans; FK `ON DELETE RESTRICT`) + relations + хелпер `_shared`.
- [x] **S2** — ✅ первая миграция `drizzle/0000`, накатана в dev, 6 таблиц проверены в БД (psql).

### A. Модуль auth (регистрация/вход) — ядро
- [x] **A1** — ✅ нижние слои account собраны и связаны (снизу вверх, каждый слой = подэтап + файл):
  - [x] **A1.1** — ✅ интерфейсы `Account`: Pure→Base→Full (+ Read=Omit secrets, Create=Base) в `modules/account/interfaces/`. Ключи Full=14 колонок, схема не сломана.
  - [x] **A1.2** — ✅ VO `Login`/`Alias`/`Password` (`modules/account/value-objects/`) + доменные ошибки (`shared/errors/`: DomainError+ValidationError), filter мапит DomainError→конверт. Проверено вживую.
  - [x] **A1.3** — ✅ `HashService` argon2id (`shared/services/hash.service.ts`, `hash()`/`verify()`, наружу только строки) + глобальный `SharedModule` (подключён в AppModule). Проверено: build, hash→verify roundtrip, boot с DI.
  - [x] **A1.4** — ✅ порт `AccountRepositoryPort` + токен `ACCOUNT_REPOSITORY` (`modules/account/adapters/`): findById, findByLoginNormalized, existsByLoginNormalized, create, updateWithVersion (CAS), decrement/incrementInvitesRemaining. Тип `AccountMutable`. Без ORM.
  - [x] **A1.5** — ✅ `AccountRepository implements AccountRepositoryPort` (`database/repositories/account/`): find/exists/create/updateWithVersion(CAS)/decr-incr квоты, маппинг row→`AccountFull`, инъекция DRIZZLE. Проверено вживую против БД (включая CAS-конфликт и атомарный счётчик).
  - [x] **A1.6** — ✅ `account.module.ts` (DI: `ACCOUNT_REPOSITORY`→`AccountRepository`, exports токен), подключён в AppModule. Boot проверен (DI-граф резолвится).
- [x] **A2** — ✅ `RegisterAccount` (free-режим) + флаги/режим. Полная 5-слойка через HTTP проверена вживую (register 201, валидация 400, БД). Invite-ветка — на этапе I.
  - [x] **A2.1** — ✅ `AccountDomainService.createAccount` (`modules/account/domain-services/`): проверка логина + хеш + `generateId` + `repo.create`, квота из ENV; `LoginTakenError` (409); экспортнут из `account.module`. Проверено вживую. TODO про гонку UNIQUE(lower(login)).
  - [x] **A2.2** — ✅ use-cases `auth` (`modules/auth/use-cases/`): `RegisterAccountUseCase` (free; raw→VO; кросс-домен вниз→account; passwordHash срезан; invite→TODO/InviteRequiredError) + `GetFeatureFlags`/`GetRegistrationMode`. Проверено вживую.
  - [x] **A2.3** — ✅ `RegisterDto` (zod `.strict()` closed-shape, `modules/auth/dtos/`) + кастомный `ZodValidationPipe` (`shared/pipes/`, без nestjs-zod). Типы ответов — FeatureFlags/RegistrationMode/AccountRead (есть). Проверено вживую (валидный/битый/лишнее поле).
  - [x] **A2.4** — ✅ controllers `auth` (`modules/auth/controllers/`): `AuthController` (POST register с zod-пайпом→RegisterResponse, GET registration-mode), `FeatureFlagsController` (GET). Прямой тест ок; HTTP live — на A2.5.
  - [x] **A2.5** — ✅ `auth.module` (controllers+use-cases, import `AccountModule`) + подключён в AppModule. Live HTTP: register 201 (полная цепочка→БД), валидация 400+details, флаги/режим 200.
- [x] **A3** — ✅ `LoginAccount` + JWT(access) + sessions(refresh httpOnly) + Guard. Весь auth-флоу через HTTP проверен (login Set-Cookie HttpOnly, refresh-ротация, reuse→401, logout). Бан-чек login-allowed — на I2 (TODO).
  - [x] **A3.1** — ✅ deps `@nestjs/jwt`+`cookie-parser` + `AccessTokenService` (`modules/auth/services/`: sign(accountId)→JWT, verify→accountId, бросает на невалидном). Секрет/TTL — в JwtModule (A3.6). Проверено вживую.
  - [x] **A3.2** — ✅ слайс sessions: порт+токен, Drizzle-репо (create/findByTokenHash/rotate-CAS/revoke), `SessionDomainService` (token_hash=SHA-256; create/rotate+reuse-detect→revoke all/revoke), `sessions.module`. Utils token/duration, `InvalidRefreshError`. Проверено вживую. TODO: lineage для реплея старого токена.
  - [x] **A3.3** — ✅ `AccountDomainService.authenticate` (lower(login)+argon2 verify+deleted/deactivated; `BadCredentialsError` 401; ban→TODO I2) + `LoginDto` + `LoginAccountUseCase` (account↓→access JWT+sessions↓). Проверено вживую.
  - [x] **A3.4** — ✅ `RefreshTokensUseCase` (sessions.rotate↓→новый access+refresh; rotateSession отдаёт accountId) + `LogoutUseCase` (revoke). Проверено: ротация/старый refresh→401/logout→401.
  - [x] **A3.5** — ✅ `AuthGuard` (Bearer→verify→getActiveById→req.account) + `cookie-parser` + `auth.controller` login/refresh/logout (refresh в httpOnly+SameSite=Lax+Path cookie). Live HTTP проверено.
  - [x] **A3.6** — ✅ `auth.module` (JwtModule из конфига, import SessionsModule+AccountModule, providers use-cases+AccessTokenService+AuthGuard export). Роуты смаппились, флоу работает.
- [⏸️] **A4** — ⏸️ **ОТЛОЖЕН: Jest/unit-тесты пока не пишем** (решение Elmir; верификация — live-прогонами node/HTTP). Подэтапы ниже — план на момент возврата. Вернуться, когда скажет Elmir.
  - [ ] **A4.1** — Jest-инфраструктура: проверить конфиг (есть в package.json), хелперы/фабрики моков портов (`AccountRepositoryPort`/`SessionRepositoryPort`), фикстуры (валидный AccountFull). `nest/test/` или `*.spec.ts` рядом.
  - [ ] **A4.2** — unit VO + utils: `Login`/`Alias`/`Password` (валидные/невалидные/normalize/equals), `parseDurationMs`, `sha256Hex`/`generateOpaqueToken`, `generateId` (формат).
  - [ ] **A4.3** — unit domain-services (моки портов): `AccountDomainService` (createAccount: LOGIN_TAKEN/квота; authenticate: BAD_CREDENTIALS/deleted/deactivated), `SessionDomainService` (create/rotate+reuse→revoke all/revoke).
  - [ ] **A4.4** — unit use-cases (моки domain-services): `RegisterAccount` (free / invite-only→INVITE_REQUIRED), `LoginAccount`, `Refresh` (нет cookie→401), `Logout`.
  - [ ] **A4.5** — (опц.) e2e supertest на тестовой БД: register→login→refresh→logout (если будет время; иначе перенести).

### I. Инвайты + дерево + баны
- [x] **I1** — ✅ invite_codes + invitations (квота, транзакции), снизу вверх. Решение по транзакциям: квота создания/отзыва инвайта — атомарный счётчик + компенсация; регистрация по коду — настоящая транзакция (`TransactionRunner`). Все I1.1–I1.5 закрыты и проверены вживую.
  - [x] **I1.1** — ✅ иерархии `InviteCode`/`Invitation` Pure→Base→Full(+Create) (схема не сломана) + VO `InviteCodeValue` (generate безопасный алфавит / create нормализация+валидация). Проверено вживую.
  - [x] **I1.2** — ✅ `InviteRepositoryPort`+Drizzle-репо (createCode/findActiveCodeByValue/deleteCode/insertInvitation/listInviteesByInviter, tx-aware). **Транзакции**: опаковый `Transaction` + `TransactionRunner` (shared-порт + Drizzle-раннер в DatabaseModule) — домен оркестрирует tx без ORM. Проверено: atomic consume + rollback.
  - [x] **I1.3** — ✅ `InviteDomainService`: createCode (код+срок, БЕЗ счётчика — кросс-домен у use-case), revokeCode (владение→delete), consumeCode (в tx: delete-гард одноразовости + insertInvitation), checkCode, listInvitees. `InviteInvalidError`. Проверено вживую (включая double-consume→INVITE_INVALID).
  - [x] **I1.4** — ✅ DTO+use-cases+controller invites: Create/Revoke/Check/ListMyInvitees (`POST/DELETE /invites`, `POST /invites/check` public, `GET /invites` под Guard) + `invites.module`+AppModule. Квота через **компенсацию** (decrement атомарен; на сбое — increment назад), не tx. `QuotaExceededError` (403). Попутно: первый защищённый чужим модулем роут вскрыл DI-нюанс — guard из `@UseGuards` инстанцируется в DI-скоупе модуля-контроллера, поэтому `AuthModule` теперь экспортит `AccessTokenService`+реэкспорт `JwtModule` (не только `AuthGuard`). Проверено вживую: create→201 (квота 3→2), check valid, #4→403 QUOTA_EXCEEDED, revoke→204, check после revoke→valid:false, #5→201, no-auth→401.
  - [x] **I1.5** — ✅ invite-режим регистрации (**TODO A2.2 закрыт**): `createAccount`/account-репозиторий теперь принимают `tx?`; `RegisterAccountUseCase` invite-ветка → `TransactionRunner.run`: createAccount(source='invite') + `consumeCode` (ребро приглашения) атомарно. Попутно **ADR-0037**: контроль доступа вынесен в `AccessControlModule` (guard+access-token), разрывает цикл `AuthModule⇄InvitesModule` (auth-флоу зависит вниз от invites ради регистрации по коду). Проверено вживую (2 фазы): без кода→403 INVITE_REQUIRED, плохой код→400 + откат (аккаунт не создан), валидный→201 source=invite + ребро, повтор→400, inviter видит приглашённого; БД: 1 ребро, 0 живых кодов.
- [x] **I2** — ✅ InviteTree (рекурсивный CTE) + bans + бан-чек login/Guard, снизу вверх. Таблица `bans` была уже в миграции 0000. Все I2.1–I2.5 закрыты и проверены вживую; **TODO A3.3/A3.5 закрыты** (бан-чек вынесен в login-use-case + Guard, account-домен остался bans-agnostic).
  - [x] **I2.1** — ✅ `InviteTreeRepositoryPort` + рекурсивный CTE (вверх по `invitations.inviter_id`, первый `db.execute` в проекте) `isAncestor` в `database/repositories/invite-tree/`. `InviteTreeDomainService` — точка кросс-домена вниз; экспортнут из `InvitesModule`. Проверено вживую (транзитивный/прямой предок→разрешён, вверх/сам→запрещён).
  - [x] **I2.2** — ✅ bans-домен: interfaces Pure→Base→Create + Full переписан на наследование (`active` — системное, в Full) + `BanRepositoryPort`+Drizzle-репо: `createBan` (идемпотентно через `onConflictDoUpdate` на partial-unique — повтор обновляет причину, тот же id), `deactivateOwn`, `existsActiveByTarget`, `listActiveByTarget`, `listByBanner`; `bans.module`. Попутно: `AccessControlModule` реэкспортит `AccountModule` (зависимость guard'а едет с ним — иначе bans-контроллер не резолвил бы `AccountDomainService`).
  - [x] **I2.3** — ✅ `BanDomainService` (idempotent ban / unban своей записи) + use-cases (`BanUser`: `isAncestor`↓ через InviteTree, нельзя себя/вверх; `UnbanUser`; `ListMyBans`) + `create-ban.dto` + `BansController` (`POST/DELETE /bans`, `GET /bans`, Guard) + `BanForbiddenError`(403)/`BanNotFoundError`(404). Проверено вживую (9 сценариев + БД: идемпотентность, владение, история снятых).
  - [x] **I2.4** — ✅ login учитывает бан (**TODO A3.3 закрыт**): `AccountBannedError` (403 `ACCOUNT_BANNED`, `details.bans`=банивший+причина, ADR-0012) + `DomainError.details` + прокидка в фильтре. `LoginAccountUseCase` оркестрирует `authenticate`↓ + `bans.listActiveAgainst`↓ → бросает с деталями. Проверено вживую (login забаненного → 403 с details).
  - [x] **I2.5** — ✅ Guard учитывает бан (**TODO A3.5 закрыт**) + **ADR-0038**: бан-чек живёт в оркестраторах (login + Guard), цикл разорван выносом `BanCoreModule` (логика банов без зависимости от AccessControl; реэкспортнут из `AccessControlModule` для guard'а). Бан действует **немедленно**. Проверено вживую: забаненный со старым валидным токеном → 403 на защищённом роуте; разбан восстанавливает доступ.

### R. Восстановление + профиль + сессии
- [x] **R1** — ✅ восстановление по секретным вопросам (ADR-0008: 1:N вопросы, K-of-N, вне регистрации). Новый модуль `recovery`. Все R1.1–R1.5 закрыты и проверены вживую (сквозной сброс пароля: старый→401/новый→200). Открытые TODO: чёрный список тем вопросов (согласовать список), анти-энумерация логинов на /start (rate-limit F5), отзыв сессий после сброса (R3).
  - [x] **R1.1** — ✅ слайс `recovery`: `SecretQa` Pure→Base→Full(+Create) (stub развёрнут; `question` в Pure, `accountId`/`answerHash` в Base, метки в Full) + view-типы (`SecretQaView`, `RecoveryQuestion` = `{id,question}` без answerHash). VO `SecretAnswer` (trim+lower+схлоп — стабильный хеш) и `SecretQuestion` (длина 8–200; чёрный список тем — TODO до согласования списка). Типы переехали из `auth/interfaces`, импорт схемы поправлен.
  - [x] **R1.2** — ✅ `SecretQaRepositoryPort` + Drizzle-репо (`add`, `listByAccount`, `removeOwn`, `countByAccount`, `findByIdsForAccount`) + `recovery.module` (биндинг токена). Проверено live (smoke по БД): add→Full, count, owner-изоляция `findByIdsForAccount`/`removeOwn`, пустой-ids гард.
  - [x] **R1.3** — ✅ `SecretQaDomainService` (addQuestion с argon2-хешем↓, removeOwn→`SecretQaNotFoundError`, listQuestions, countQuestions, pickRandomK Fisher–Yates, verifyAnswers — уникальность id + сверка ВСЕХ) + расширения `AccountDomainService`: `resetPassword` (argon2 + приватный `_applyWithRetry` CAS, `OPTIMISTIC_RETRY_ATTEMPTS`), `setRecoveryRequiredCount` (CAS; `1≤K≤N` валидирует use-case), `findRecoveryAccountByLogin` (узкая проекция `{id,recoveryRequiredCount}` без секретов, исключает удалённых). Проверено live (smoke): нормализация ответа → стабильный хеш (барсик≡«  Барсик »), reset (старый отвергнут/новый принят), owner-изоляция, дубликат/чужой id → false.
  - [x] **R1.4** — ✅ settings (Guard): use-cases Add/Remove/ListMySecretQuestions + SetRecoveryRequiredCount (кросс-домен: count↓ + `1≤K≤N` + account↓) + DTO + `RecoverySettingsController` (`GET/POST /recovery/questions`, `DELETE /recovery/questions/:id`, `PUT /recovery/required-count`) + `RecoveryRequiredCountInvalidError`(422). Проверено live: листинг без answerHash, no-token→401, K>N→422.
  - [x] **R1.5** — ✅ recovery-флоу (public): StartRecovery (`login`→K случайных) + CompleteRecovery (verify все K → `resetPassword`↓) + DTO + `RecoveryController` (`POST /recovery/start`, `/complete`) + `RecoveryNotAvailableError`(409)/`RecoveryFailedError`(401). `RecoveryModule` в AppModule. Проверено live: сквозной сброс (старый→401/новый→200), неверный ответ/недобор→401, несуществующий логин→409. TODO: анти-энумерация (rate-limit F5), отзыв сессий после сброса (R3).
- [x] **R2** — ✅ профиль + жизненный цикл аккаунта + аватар (ADR-0017 soft-delete, ADR-0031/0032/**0040** файлы). Новый модуль `profile`. Все R2.1–R2.5 закрыты и проверены вживую. TODO: отзыв сессий после deactivate/delete (R3).
  - [x] **R2.1** — ✅ модуль `profile` (вне `account.module`, без цикла): `AccountPublicView`={login,alias,avatar} + `GetMe` (инлайн-проекция request.account→`AccountRead`) / `GetProfileByLogin` (публичная; `AccountNotFoundError` 404) + account-домен `getPublicByLogin` (исключает удалённых) + `ProfileController` (`GET /accounts/me`, `GET /accounts/:login`, Guard, `me` объявлен до `:login`). `ProfileModule` в AppModule. Проверено live.
  - [x] **R2.2** — ✅ patch-alias: `UpdateAliasUseCase` + DTO + `PATCH /accounts/me` (Guard); account-домен `updateAlias` через `_applyWithRetry` (CAS) → `AccountRead`. Проверено live: alias обновился, version 0→1; невалидный (2 симв.)→400; приватное наружу не уходит.
  - [x] **R2.3** — ✅ жизненный цикл (**ADR-0039**): account-домен `deactivate`/`reactivate`/`softDelete` (CAS) + `authenticateForReactivation`; `authenticate` теперь различает deactivated→403 `ACCOUNT_DEACTIVATED` (deleted→401 терминально). Само-действия под Guard: `POST /accounts/me/deactivate`, `DELETE /accounts/me` (ProfileModule). Реактивация — публичный `POST /auth/reactivate` {login,password} (деактивированный не проходит Guard). Проверено live (10 шагов): deactivate→Guard блок старого токена→login 403→reactivate→login 200→delete→login 401 BAD_CREDENTIALS→reactivate удалённого 401. TODO: отзыв сессий после deactivate/delete (R3).
  - [x] **R2.4** — ✅ инфра аватара (**ADR-0040**): `CONTENT_DIR` в env + раздача `content/` статикой под `/content/` (main.ts `NestExpressApplication.useStaticAssets`, без новых зависимостей) + `AvatarStoragePort` (save/delete за абстракцией, fs не в домене — чище WC2, задел под S3) + `AvatarDiskStorage` в `system/storage/` (имя `generateId().<ext>`). `content/` в .gitignore. Выбор vs обобщённый files-реестр WC2 — оставили простую модель (Elmir), реестр отложен на фазу 3.
  - [x] **R2.5** — ✅ аватар-эндпоинты: `POST /accounts/me/avatar` (`FileInterceptor`, тип по magic-bytes jpeg/png/webp + `AVATAR_MAX_BYTES`, хард-кап интерцептора, порядок новый→БД→старый + компенсация) + `DELETE /accounts/me/avatar` (БД=null→удалить файл) + account-домен `setAvatar` (CAS) + `AvatarInvalidError`(400). Проверено live: upload→статика 200, замена стирает старый (404)/новый 200, не-картинка→400, delete→null+файл 404, без токена→401, 0 остатков на диске.
- [x] **R3** — ✅ сессии: список устройств + отзыв (своя/остальные) + интеграция отзыва. Все R3.1–R3.3 закрыты и проверены вживую. «Текущая» сессия — через sid в access-JWT (ADR-0041). Отложенные TODO про отзыв сессий (deactivate/delete/recovery-reset) — закрыты.
  - [x] **R3.1** — ✅ sid в access-JWT (**ADR-0041**): `AccessTokenPayload += sid`; `sign(accountId, sessionId)` + verify→`{accountId,sessionId}`; `createSession`/`rotateSession` отдают `sessionId` (ротация сохраняет тот же id строки); login/refresh кладут sid; `AuthGuard`→`request.sessionId`; `AuthenticatedRequest += sessionId`. Проверено live: токен несёт sub+sid, защищённый роут 200, **sid стабилен через refresh** (SID1==SID2), sid==id строки сессии в БД.
  - [x] **R3.2** — ✅ список + отзыв своей: порт `listActiveByAccount`+`revokeByIdForAccount` + domain `listActive`/`revokeOwn` (владение `accountId==me`) + `SessionView`={id,userAgent,createdAt,expiresAt,current} (current по `request.sessionId`) + use-cases ListMySessions/RevokeSession + `SessionsController` (`GET /sessions`, `DELETE /sessions/:id`, Guard; sessions.module импортит AccessControlModule) + `SessionNotFoundError`(404). Проверено live: 2 устройства, current только у своей, отзыв чужого→204 (его refresh→401), повтор/чужой/без токена→404/404/401.
  - [x] **R3.3** — ✅ revoke-others + интеграция: порт `revokeAllByAccountExcept` + domain `revokeOthers`/`revokeAllForAccount` + `DELETE /sessions/others` (Guard, `others` объявлен до `:id`). **Отложенные TODO закрыты**: deactivate/delete (ProfileModule↓ sessions) и recovery-reset (CompleteRecovery↓ sessions) отзывают все сессии — Profile/Recovery импортируют SessionsModule (цикла нет). Проверено live: 3 устройства→others→1 (current жив, остальные refresh 401); deactivate гасит все (refresh 401, в БД активных 0).

### F. Фронт ЛК
> Спека вида/адаптива/UX-форм/экранов (вкл. главную и app-shell) — [`docs/ui-ux.md`](./ui-ux.md) (дополнена анализом лучших практик 2025/26). Тех.правила — [`docs/frontend.md`](./frontend.md). Философия: минимализм/ламповость, без ПДн, тёмная+тумблер, чистый SCSS, Signals, Material только `MatDialog`. Поток: cookie-гейт → главная → auth → app-shell(разделы).

- [x] **F1** — ✅ core (невидимая инфра): F1.1–F1.6 закрыты. Angular 21 standalone + Material(MatDialog)/анимации; дизайн-токены/тема (ThemeStore); AuthStore (Signals); HTTP-слой (interceptor 401→refresh, guard, feature-flags, session-restore); модальная система (ADR-0026); shared UI (button/text-field+password-toggle/card/banner/empty-state/theme-toggle/spinner). `ng build` (dev+prod) зелёный. Визуальный прогон компонентов/модалок — на F2.
  - [x] **F1.1** — ✅ проект уже был scaffold'ен (Angular 21 standalone, SCSS, strict с `exactOptionalPropertyTypes`/`noUncheckedIndexedAccess`). Добавлено: `@angular/material`+`cdk`+`animations` (21.2.x, под `MatDialog`); провайдеры `provideHttpClient(withFetch())` (под F1.4) + `provideAnimationsAsync()`; каркас lazy-`routes` (публичная/приватная зоны — заглушки под F2/F3). `ng build` зелёный.
  - [x] **F1.2** — ✅ дизайн-токены: `src/styles/_tokens.scss` (CSS-переменные: палитра тёмная-дефолт + `.theme-light`-override, радиусы, типографика, шкала отступов, `--touch-min:44`), `_breakpoints.scss` (миксин `up()` 480/768/1024/1280), `_reset.scss` (+видимый фокус AA); `angular.json` includePaths=`src/styles`. `ThemeStore` (Signal): класс на `<html>` + localStorage, дефолт тёмная, инжектится в `App`. `ng build` зелёный.
  - [x] **F1.3** — ✅ `core/auth`: `AuthStore` (Signal — account + access-токен **в памяти**, `isAuthenticated`, set/clear) + `core/interfaces/account-read` (зеркало подмножества). `generateId` util — **уже был** (задел, `_initial` от scaffold). NB: в фазе 1 фронт **не генерит id по контракту** — все PK генерит бэк и возвращает в ответе; util — задел под оптимистичный UI / клиентскую идемпотентность / офлайн-нативку (фаза 6), без потребителя сейчас.
  - [x] **F1.4** — ✅ HTTP-слой: `authInterceptor` (base `environment.apiBase`+`API_PREFIX`, `withCredentials`, Bearer из сигнала; `401`→**single-flight** `POST /auth/refresh`→повтор; refresh-fail→clear+`/auth/login`) + `authGuard` (CanActivateFn) + `FeatureFlagsStore` + `sessionInitializer` (`provideAppInitializer`: грузит флаги + **тихо восстанавливает сессию** по refresh-cookie → `GET /accounts/me`, чтобы reload не выкидывал на login) + environments (apiBase dev `:3000`/prod `''` через fileReplacements). dev+prod build зелёные. Браузерный runtime-прогон интерсептора — на F2 (когда login его задействует).
  - [x] **F1.5** — ✅ модальная система (`shared/modals`, ADR-0026): `DialogModalComponent` (Способ A) + `DialogModalData<T>`/`ModalHeaderClassIcon` + партиалы header(CSS-иконки, без ассетов)/content/footer + `SpinnerModalComponent` (Способ 5) + базовый `ModalService` (confirm/success/error/openLoading) + constants. MatDialog подключён: `@angular/cdk/overlay-prebuilt.css` в angular.json + `_dialog.scss`/`_buttons.scss` (MDC под наши токены, глобальные `.btn`). svgIcon-ветка отложена (нет svg-icon компонента). `ng build` зелёный. Реальный показ модалок — на F2/F3.
  - [x] **F1.6** — ✅ shared UI на SCSS (standalone, signal-inputs): `app-button` (accent/ghost/danger/loading/block, глобальные `.btn`), `app-text-field` (label+input+ошибка, **password show/hide**, `aria-invalid`/`aria-describedby`, работает с `FormControl`), `app-card`, `app-banner` (info/success/warning/danger), `app-empty-state`, `app-theme-toggle` (ThemeStore), `app-spinner`. Без Material. `ng build` зелёный (компоненты типизированы; tree-shaken до использования в F2/F3).
- [x] **F2** — ✅ публичные экраны: F2.1–F2.6 закрыты. Cookie-гейт → главная → register → login → recover. Весь публичный флоу работает; `ng build` зелёный. Браузерная проверка — на запущенном стеке.
  - [x] **F2.1** — ✅ auth-плумбинг: `PublicLayoutComponent` (центр+тумблер темы+outlet) + `auth.routes` (lazy, дети — заглушки под F2.3-F2.6) подключён в `app.routes` (`''`); `AuthApiService` (register/login/logout/reactivate/registrationMode/checkInvite/recoveryStart/recoveryComplete) + `auth.types` (зеркала); `errorMessage(code)`/`errorCode(error)` (RU-маппинг конверта `{error:{code,message}}`); `guestGuard`. `ng build` зелёный.
  - [x] **F2.2** — ✅ cookie-гейт (ADR-0024, блокирующий): `ConsentStore` (Signal+localStorage) + `ConsentGateComponent` (card+2 кнопки, ссылка на /privacy); `App` рендерит роуты только при согласии, иначе гейт. **Первый видимый UI** (использует `app-card`/`app-button`/`app-theme-toggle`). `ng build` зелёный.
  - [x] **F2.3** — ✅ **главная (landing)**: `LandingComponent` («Нормисы» + вайб + 2 CTA `<a class="btn">` → `/login`/`/register`, адаптив столбик→строка через `bp.up('sm')`, ≥44). Тумблер темы — в публичном layout. Подключена как `''` в auth.routes. `ng build` зелёный (lazy-чанк). Видна на `localhost:4200` после cookie-гейта. (CTA ведут на /login,/register — экраны в F2.4/F2.5.)
  - [x] **F2.4** — ✅ `RegisterComponent` (`/register`, guestGuard): шаги code→form по `freeRegistration`. invite-режим — авто `POST /invites/check` (debounce, нормализация, без кнопки → при валидном пускает к форме). Форма alias/login/password (reactive, валидаторы-зеркало бэка, `app-text-field` с password show/hide, ошибки полей). `POST /auth/register` → success-модалка + редирект `/login`. Ошибки сервера → `errorMessage` в banner. `ng build` зелёный. NB: в invite-only на пустой БД зарегистрироваться через UI нельзя (нет кодов) — для теста нужен FREE_REGISTRATION=true или сид-аккаунт с кодом.
  - [x] **F2.5** — ✅ `LoginComponent` (`/login`, guestGuard): форма → `POST /auth/login` → `setAccessToken`+`me()`→`setAccount` → редирект `/app`. `ACCOUNT_DEACTIVATED` → `ModalService.confirm` «восстановить?» → `POST /auth/reactivate` → повтор входа; `ACCOUNT_BANNED` → `ModalService.error` со списком из `details.bans`. Прочее → banner. `ng build` зелёный.
  - [x] **F2.6** — ✅ `RecoverComponent` (`/recover`, guestGuard, многошаговый): логин → `POST /recovery/start` (K вопросов в `FormArray`) → ответы + новый пароль → `POST /recovery/complete` → success-модалка + `/login`. Ошибки (`RECOVERY_NOT_AVAILABLE`/`RECOVERY_FAILED`) → banner. `ng build` зелёный.
- [ ] **F3** — аутентифицированная зона (`/app`). У каждой фронт-секции — свой API-сервис + интерфейсы-зеркала; ошибки через `errorMessage`; роуты под `authGuard`. **Бэкенд-предпосылки** (разрывы спека↔бэк) — отдельными блоками **🔧 `F3.x·БЭК`** (правки в **NestJS, НЕ фронт**; перед своим фронт-подшагом; коммит в `nest`+`docs`).
  > **IA навигации (реш. Elmir):** верхнее меню = только фичи (**Приглашения**, с вкладкой **Баны** внутри); аккаунт-стафф — в **дропдауне по клику на имя** (Профиль / Настройки / Выйти). **Устройства (сессии)** и **секретные вопросы** — внутри **Настроек** (Безопасность). Деактивация/удаление — в Настройках (Аккаунт). Тема — в Настройках + тумблер в шапке.
  - [x] **F3.1** — ✅ (фронт) app-shell + навигация + роутинг: `ShellComponent` = layout (шапка: бренд + верхнее меню фич [**Приглашения**] `routerLinkActive` + тумблер темы + **аккаунт-дропдаун** [клик на имя → Профиль/Настройки/Выйти, закрытие по backdrop] ) + `router-outlet`. `shell.routes` (layout + дети lazy: profile/invites/sessions/settings, default→profile); `/app` → `loadChildren`. Разделы — **сиды** (наполнят F3.2/F3.4/F3.6/F3.7). `u/:login` — в F3.2. `ng build` зелёный. (IA пересмотрена под аккаунт-дропдаун; сессии/вопросы уедут в Настройки, баны — в Приглашения; гамбургер-дровер отложен.)
  - [x] 🔧 **F3.2·БЭК** (БЭКЕНД, не фронт) — ✅ «кто меня пригласил» (предпосылка F3.2; разрыв: на I-этапе сделали только сторону пригласившего). Архитектурный аудит пройден (5 слоёв, чистые границы grep, кросс-домена нет, типы ADR-0033, DI через токен, дока=факт, live):
    - [x] **F3.2·БЭК.1** — ✅ invite-репо `findInvitationByAccount(accountId)` (INNER JOIN `accounts` за login/alias пригласившего; accountId уникален → ≤1 строки) → проекция `InviterRead` (`inviterLogin/inviterAlias/reason/invitedAt`); порт `InviteRepositoryPort` пополнен; domain `InviteDomainService.getInviterOf(accountId)`. ORM только в `database/` (repo импортит accounts-схему), домен видит лишь `InviterRead`. `tsc --noEmit` зелёный. Эндпоинт+live — БЭК.2.
    - [x] **F3.2·БЭК.2** — ✅ `GET /invites/my-inviter` (AuthGuard) → `InviterRead | null` (`GetMyInviterUseCase` → domain `getInviterOf`); зарегистрирован в `InvitesModule`; `api-contracts.md` синхронизирован (заодно `GET /invites` и `POST /invites` приведены к факту). Live: приглашённый → `{inviterLogin, inviterAlias, reason, invitedAt}` [200], корень free → `null` [200], без токена → 401 (JOIN-путь проверен вставкой временного ребра с последующим cleanup, т.к. dev в `FREE_REGISTRATION=true` рёбра не создаёт). NB: в free-режиме `inviteCode` при регистрации игнорируется (штатно) — реальные рёбра только в invite-only.
  - [x] **F3.2** — ✅ (фронт) profile (свой + чужой). `AccountApiService` (getByLogin/updateAlias/deactivate/deleteMe/uploadAvatar/removeAvatar; `me()` НЕ дублируем — он в `AuthApiService` для auth-флоу, свой профиль берём из `AuthStore`) + `InvitesApiService.myInviter()`. Зеркала `AccountPublicView`/`InviterRead`, util `avatarUrl()` (→ `${apiBase}/content/<path>`). my-profile: данные из стора (login/alias/дата/источник RU-подпись/квота) + «кто пригласил» из `/invites/my-inviter` (ссылка на `/app/u/:login`), инлайн-правка alias (`PATCH` → `setAccount`). user-profile `/app/u/:login` (`GET /accounts/:login`, реагирует на смену параметра, 404→empty-state). Аватар — только отображение (загрузка/кроп — F3.3). Роут `u/:login` в shell. `ng build` зелёный; live-смоук контрактов (me/patch/getByLogin/404/my-inviter) сошёлся. `api-contracts.md` синхронизирован с фактом (форма `AccountRead`/`AccountPublicView`). «Кого пригласил» (список) — invites (F3.4).
  - [x] **F3.3** — ✅ (фронт) avatar: загрузка + **кроп в модалке своим canvas** (развилка решена → [ADR-0042](./decisions/0042-avatar-crop-own-canvas.md): свой canvas, без либы). `AvatarCropModalComponent` (квадратный viewport, pan pointer-ами + zoom-слайдер с сохранением центра, клэмп cover; вывод `toBlob` jpeg 0.9 в 512×512, белый фон) + `AvatarCropService` (обёртка над MatDialog → `Promise<Blob|null>`, компоненты не дёргают MatDialog напрямую). В профиле аватар — **дропдаун-меню** (клик, паттерн shell-аккаунта + backdrop): **Посмотреть** (модалка `AvatarViewModalComponent`/`AvatarViewService` — полный размер), **Сменить/Загрузить** (скрытый file-input, accept image/* → кроп → `uploadAvatar` → `setAccount`), **Удалить** (confirm-модалка → `removeAvatar`). Overlay-спиннер на время операции, ошибки → banner. Рендер миниатюры (свой/чужой) через `avatarUrl()` (F3.2). `ng build` зелёный (crop в profile-чанке). Бэк-приём/замена/валидация — verified живьём ранее. Браузер-проверка кропа — на Elmir'е.
  - [x] 🔧 **F3.4·БЭК** (БЭКЕНД, не фронт) — ✅ alias приглашённых: `listInviteesByInviter` теперь INNER JOIN `accounts` (по `invitations.accountId`) → `InviteeRead` стал денормализованной проекцией `{accountId, login, alias, reason, invitedAt}` (порт+domain возвращаемый тип обновлены, use-case `list-my-invites` упрощён — маппинг убран, **alias-TODO снят**). ORM только в `database/`; домен видит лишь `InviteeRead`. `tsc` зелёный; live: `GET /invites` отдаёт login/alias приглашённого (проверено временным ребром + cleanup). `api-contracts.md` синхронизирован.
  - [x] 🔧 **F3.4·БЭК.2** (БЭКЕНД, не фронт) — ✅ список pending-кодов (предпосылка полноценного отзыва; реш. Elmir 2026-06-08): `GET /invites/codes` → `InviteCodeRead[]` (`{id, code, reason, expiresAt}`, активные невыданные). Репо `listCodesByInviter` (where inviterId + не истёк) + порт + domain `listMyCodes` + `ListMyCodesUseCase`. Роут `codes` объявлен — корень `GET /invites` не перехвачен. `tsc` зелёный; live: создал 2 → codes=2 → отзыв → codes=1, `GET /invites`=[], квота 3-2+1=2. `api-contracts.md`.
  - [x] **F3.4** — ✅ (фронт) invites: `InvitesApiService` дополнен (`listInvitees`/`listCodes`/`create`/`revoke`) + зеркала `InviteeRead`/`InviteCodeRead`. Экран: счётчик `invitesRemaining` (из `AuthStore`), форма создания (reason 1–500 + **ПДн-предупреждение** warning-баннер) → `POST /invites` → код в списке; **невыданные коды** (`GET /invites/codes`) с **копированием** (`navigator.clipboard`, отметка «✓») и **отзывом** (confirm → `DELETE /invites/:id`); **приглашённые** (`GET /invites`) — alias-ссылка на `/app/u/:login` + login/причина/дата. После create/revoke квота освежается через `me()`→`setAccount`. Пустые состояния, ошибки → banner. `ng build` зелёный; контракты live-проверены (F3.4·БЭК/.2). Браузер-проверка (copy/clipboard) — на Elmir'е. Вкладки «Мои инвайты»/«Баны» — F3.5.
  - [x] 🔧 **F3.5·БЭК** (БЭКЕНД, не фронт) — ✅ данные для банов (предпосылка F3.5). Архитектурный аудит пройден (чистые границы grep, 5 слоёв, кросс-домена нет, типы ADR-0033, DI через токен, дока=факт, live):
    - [x] **F3.5·БЭК.1** — ✅ `AccountPublicView += id` (`Pick<AccountFull,'id'|'login'|'alias'|'avatar'>`; id не ПДн, нужен как `targetId` для бана из карточки `/app/u/:login`). `getPublicByLogin` отдаёт `id`; репо не трогали (проекция из полного аккаунта). `tsc` зелёный; live: `GET /accounts/:login` вернул `id` (совпал с БД). `api-contracts.md` синхронизирован.
    - [x] **F3.5·БЭК.2** — ✅ «мои баны» с именем цели: `listByBanner` INNER JOIN `accounts` (по `bans.targetId`) → новый тип `BanListItem` = `BanView + {targetLogin, targetAlias}` (порт/domain/use-case/контроллер `GET /bans` обновлены, маппинг убран). **Отступление от плана (осознанно):** имена кладём в отдельный list-тип, а НЕ в общий `BanView` — `POST /bans` оставлен на `BanView` без лишнего фетча аккаунта (банящий цель уже знает). `tsc` зелёный; live: `GET /bans` вернул `targetLogin/targetAlias` (вставка строки бана + cleanup). `api-contracts.md` синхронизирован (заодно убран дрейф: `POST` форма ответа, `DELETE /bans/:id`, `GET /bans`, код `BAN_FORBIDDEN`).
  - [x] **F3.5** — ✅ (фронт) bans. `BansApiService` (`create`/`listMine`/`unban`) + зеркала `BanView`/`BanListItem`. **Приглашения → табы** «Мои инвайты» / «Баны» (signal-таб в `InvitesComponent`). **«Забанить» в карточке** `/app/u/:login` (инлайн-форма: reason 1–500 + ПДн-предупреждение → `POST /bans`; прячется на своём профиле через `isOwnProfile`; ошибка/`BAN_FORBIDDEN` → banner; успех → «Участник забанен»). Фронтовый `AccountPublicView += id` (targetId). **Вкладка «Баны»** = `BansComponent` (`GET /bans` с именами цели, alias-ссылка на профиль, статус активен/снят, **разбан** `DELETE /bans/:id` с confirm + оптимистичное active=false). `ng build` зелёный; смоук: self-ban→`BAN_FORBIDDEN`, `GET /bans`→[], разбан несуществующего→404. **Поправлен дрейф, который сам внёс в .2:** `DELETE /bans/:id` не «204 no-op», а 404 `BAN_NOT_FOUND` при не-найдено. Браузер-проверка бан-флоу (нужно дерево приглашений) — на Elmir'е.
    - **Фикс (восстановление сессии забаненным):** `authInterceptor` глобально ловит `403 ACCOUNT_BANNED` на защищённых запросах (`/accounts/me` и т.п.) → сбрасывает сессию, уводит на `/`, показывает модалку «вы забанены: за что» (`details.bans`). Раньше забаненный с живой refresh-сессией висел на спиннере и не видел причину. Credential-эндпоинты (login/…) исключены — их бан показывает сам компонент. Заодно исправлен латентный редирект refresh-fail `/auth/login`→`/login`.
  - [x] 🌳 **F3.Д — дерево приглашений (ленивое раскрытие)** ✅ (БЭК.1+БЭК.2+фронт; архаудит пройден: границы grep, 5 слоёв, без `any`, типы ADR-0033, дока=факт, live). **Зачем:** право банить — на всё своё **поддерево** (`isAncestor`, транзитивно), но в UI видны только **прямые** приглашённые (`GET /invites` жёстко «мои») и прямой родитель (`/invites/my-inviter`); «кого пригласил тот, кого пригласил я» — негде увидеть и некого там забанить. **Решение (Elmir 2026-06-09): вариант B — ленивое раскрытие по узлу** (НЕ всё поддерево одним JSON: при росте дерева до тысяч узлов это убило бы фронт; грузим только раскрытую ветку). Граница доступа = ровно своё поддерево.
    - [x] 🔧 **F3.Д·БЭК.1** — ✅ `GET /invites/of/:accountId` → `InviteeNode[]` (`accountId/login/alias/reason/invitedAt` + **`bannedByMe`** через LEFT JOIN моих активных банов). Доступ: `accountId` = я **или** `isAncestor(me, accountId)`, иначе 403 `SUBTREE_FORBIDDEN`. Полная 5-слойка: интерфейс `InviteeNode`, репо `listInviteesOf` (join `accounts` + isNull deletedAt + left join `bans`), порт, domain-проброс, use-case `ListNodeInviteesUseCase` (permission через `InviteTreeDomainService.isAncestor`), эндпоинт. `tsc` зелёный; live (elmir→test1): дети узла + `bannedByMe` true/false (бан+снятие, восстановлено), доступ к потомку, 403 для чужого поддерева, роут не перехвачен. `api-contracts.md`.
    - [x] 🔧 **F3.Д·БЭК.2** — ✅ `GET /invites/can-ban/:accountId` → `{ allowed }` (обёртка над `isAncestor`, `target ≠ me`). Без правок репо/порта/домена — use-case `CanBanUseCase` зовёт `InviteTreeDomainService.isAncestor` + self-чек, эндпоинт. `tsc` зелёный; live: elmir→test1 `true`, elmir→self `false`, prof→elmir `false` (= кейс test1→elmir). `api-contracts.md`.
    - [x] **F3.Д** — ✅ (фронт) вкладка **«Дерево»** (3-я в Приглашениях): `InviteTreeComponent` — корень = мои прямые приглашённые (`GET /invites/of/:myId`), каждый узел разворачивается **по клику** (лениво `GET /invites/of/:accountId`, грузим только открытую ветку), плоский список с глубиной (ключ — `accountId`, уникален в дереве), бейдж «забанен вами», «никого не пригласил» для пустых, клик по узлу → карточка. `InvitesApiService` += `listOf`/`canBan` + зеркала `InviteeNode`/`CanBanResponse`. **+ Фикс фантом-кнопки:** карточка `/app/u/:login` тянет `can-ban` → блок модерации виден только при `myBan ∨ canBanTarget`, «Забанить» — только при праве (test1 на профиле elmir_kuba больше не видит кнопку). `ng build` зелёный; контракты live-проверены (БЭК.1/.2). Браузер-проверка раскрытия — на Elmir'е.
  - [x] **F3.6** — ✅ (фронт) sessions: `SessionsApiService` (`listMine`/`revoke`/`revokeOthers`) + зеркало `SessionView`. `SessionsComponent` — список устройств (`userAgent`/дата входа/срок, бейдж **«это устройство»** по серверному `current`), **«Завершить»** на чужих (`DELETE /sessions/:id`, confirm), **«Выйти на остальных»** (`DELETE /sessions/others`, confirm, при наличии других). Текущую тут не отзываем (для выхода — «Выйти» в шапке). Самодостаточный компонент (наполнил сид `/app/sessions`); F3.7 встроит во вкладку «Безопасность». `ng build` зелёный; live: `GET /sessions` форма+`current` корректны, `DELETE` несуществующей→404 `SESSION_NOT_FOUND`. `api-contracts.md` синхронизирован (был дрейф: `expiresAt`, `DELETE /sessions/others` вместо `POST revoke-others`).
  - [x] 🔒 🔧 **F3.6·БЭК — немедленный отзыв сессии (security)** ✅ ([ADR-0043](./decisions/0043-session-liveness-in-guard.md)). Дыра: `AuthGuard` проверял подпись+аккаунт+бан, но НЕ живость сессии → кикнутое устройство работало до истечения access-токена (~15 мин: могло банить, ходить в профиль/приглашения). Фикс: Guard проверяет `SessionDomainService.isActive(sid)` (есть/не отозвана/не истекла) на каждый запрос → отзыв мгновенный (401 «Сессия завершена.»). Репо `existsActiveById` + порт + domain `isActive`. Граница модулей: вынесен `SessionCoreModule` (зеркало `BanCoreModule`) — Guard и контроллер `/sessions` используют ядро без цикла. Refresh уже отвергал отозванные → кикнутый не перевыпустит токен; фронт не трогали (401→интерсептор→refresh-fail→logout). Nest стартует чисто; live: вход#1 200 → отзыв со входа#2 → вход#1 сразу **401**, вход#2 200. (Реш. Elmir 2026-06-09: вариант A; D/WebSocket — опц. UX потом.)
  - [x] **F3.7** — ✅ (фронт) settings (хаб из аккаунт-дропдауна), вкладки Безопасность/Аккаунт/Тема. Разбит:
    - [x] **F3.7.1** — ✅ каркас `SettingsComponent` (signal-табы `@switch`) + **Тема** (тумблер `app-theme-toggle` + пояснение) + **Аккаунт** (деактивация `POST /accounts/me/deactivate` / удаление `DELETE /accounts/me`, confirm-модалки → `clear`+редирект на `/`) + **Безопасность** = встроенный `app-sessions` (F3.6). `ng build` зелёный; live (round-trip на throwaway): deactivate→204, старый токен→**401** (ADR-0043), повторный вход→`ACCOUNT_DEACTIVATED` (модалка восстановления F2.5), reactivate→восстановлено. Секретные вопросы — F3.7.2.
    - [x] **F3.7.2** — ✅ в **Безопасность**: `RecoverySettingsComponent` + `RecoverySettingsApiService` (`listQuestions`/`addQuestion`/`removeQuestion`/`setRequiredCount`) + зеркало `SecretQaView`. Над сессиями: список вопросов (удаление с confirm), форма добавления (вопрос+ответ 1–300), select **K** (1..N, текущее из `recoveryRequiredCount`, после изменений `me()`→`setAccount`), баннер-напоминание пока не настроено. `ng build` зелёный; live (throwaway+cleanup): list/add (`{id,question}`)/K=1 (me=1)/K=5→422 `RECOVERY_REQUIRED_COUNT_INVALID` (фронт-select это и не даёт)/delete. `api-contracts.md` синхронизирован (`requiredCount`, POST-форма, 204).

### F4. Главный экран (overview) — обсудить ПОСЛЕ всех F (и F3.*)
- [x] **F4** — ✅ обзор/дашборд (`/app/overview`, дефолт `/app`; бренд «Нормисы» ведёт сюда). Сетка **метрик-карточек** (большое число + подпись, акцент-градиент) + **донат** «полезность». **Без спарклайнов/трендов** в фазе 1 — для них нужна time-series история (снимки метрик), которой не храним; добавим позже отдельной таблицей-снапшотом (future). Кубики будущих сервисов (Акцент/Музыка/Финансы) — фазы 2+.
  - **Решения (Elmir 2026-06-09):** подход **A** — один `GET /stats/overview` (сервер считает `COUNT`-ами, фронт 1 запрос). Кубики фазы 1: **Пользователей всего**, **Вы пригласили** (прямых + поддерево), **Полезность приглашённых** (донат: активны/забанено), **Статус аккаунта** (осталось приглашений / активных устройств / восстановление настроено).
  - [x] 🔧 **F4·БЭК** — ✅ `GET /stats/overview` → `OverviewStats` (10 полей). **Минимум нового:** реально новых COUNT — **два** (`AccountRepository.countActive` + `InviteTreeRepository.countDescendants` рекурсивный CTE); остальное use-case считает из длины существующих list-методов (`listInvitees`/`listMyCodes`/`listMine`(bans)/`listActive`(sessions)/`countQuestions`), полезность — пересечение приглашённых×активных банов в use-case, `invitesRemaining`/`recoveryRequiredCount` — из аккаунта Guard'а. Новый `StatsModule` (лист графа) + 1 агрегирующий use-case (единственный «3-й слой»), зовёт 6 доменов вниз (account/invites/invite-tree/bans/sessions/recovery; `RecoveryModule` экспортит `SecretQaDomainService`). Nest стартует чисто (DI ок); live: данные осмысленны, `totalUsers=10` совпал с БД, бан test1 → `inviteesBannedByMe/bansActive=1` → разбан → 0 (откат). `api-contracts.md`.
  - [x] **F4·фронт** — ✅ `OverviewComponent` (заменил заглушку) + `StatsApiService` + зеркало `OverviewStats`. Сетка метрик-карточек (Нас всего [accent-градиент]; Вы пригласили N · поддерево M; осталось приглашений + невыданных кодов; активных устройств → ссылка в Настройки; восстановление настроено/нет → ссылка) + **SVG-донат** полезности (без либ: track красный/arc зелёный по `usefulPercent`, легенда активны/забанено, % в центре; пусто при 0 приглашённых). Один запрос `GET /stats/overview`. `ng build` зелёный; контракт live-проверен (F4·БЭК). Браузер-проверка — на Elmir'е.

### Техдолг (вне фаз, разобрать отдельно)
- [ ] **TD-lint (бэк)** — нормальная lint-уборка бэка. Поверх F3.2·БЭК в рабочем дереве остался незапрошенный репо-широкий `eslint --fix` sweep (10 файлов: database.constants/_shared, account/auth/recovery/sessions domain+controller, account-banned.error, all-exceptions.filter). Часть правок безобидна, но правило require-jsdoc выдало **битый код** (пустые `/** */` внутри инлайн-типов, напр. `auth.controller` `Promise<{ accessToken }>`). Сначала починить eslint-конфиг (require-jsdoc не должен лезть в члены инлайн-типов), затем прогнать `--fix` начисто и закоммитить отдельно. Базлайн до уборки — 119 lint-ошибок (преды, не от F3.2·БЭК). (Реш. Elmir 2026-06-08: оставить sweep, разобрать отдельно.)
- [ ] **TD-lint (фронт)** — `ng lint` даёт ~124 ошибки в 24 файлах (весь фронт, не только новые F3.*; `ng build` при этом чистый — 0/0). Гейт у нас = build+tsc+live, lint в него не входил → чистого `ng lint` не было с самого начала. Доминируют пере-строгие правила: `explicit-function-return-type` (требует тип даже у инлайн-лямбд роутов `loadComponent: () => import(...)`), `jsdoc/require-jsdoc`, `no-confusing-void-expression`. План уборки: смягчить пере-строгие правила в eslint-конфиге (return-type не на каждой лямбде; jsdoc по месту) + `--fix` + доаннотировать остаток → `ng lint` в ноль, затем добавить в гейт. (Реш. Elmir 2026-06-08: отложить, разобрать отдельно.)

### F5. Публичная зона: навигация / право / прозрачность / уведомления (ПЕРЕД D1)
- [ ] **F5** — pre-deploy: голую публичную зону довести до запуска «для своих». **Решения (Elmir 2026-06-09):** лицензия ПО — **Apache-2.0**; центр уведомлений — **в F5** (до деплоя). **Три РАЗНЫЕ сущности (не путать):** лицензия ПО (репо `LICENSE`, права на код/форки) ≠ Условия использования (`/terms`, правила сервиса + дисклеймер) ≠ Политика конфиденциальности (`/privacy`, данные/152-ФЗ). Cookie-баннер ссылается на политику **и** условия. Юр-тексты Opus набрасывает под философию проекта, **контент — на ревью Elmir'а** (CLAUDE.md: 152-ФЗ/юр не решаю сам; без претензии на юр-точность).
  - [x] **F5.1** — ✅ публичная навигация: `PublicLayoutComponent` получил **шапку** (бренд «Нормисы» → `/` + тумблер темы) и **футер** (ссылки О проекте / Условия использования / Политика конфиденциальности). Сид-страницы `/terms` (`TermsComponent`) и `/about` (`AboutComponent`) + общий `legal-page.scss` (контент — F5.2/F5.3); роуты под `PublicLayoutComponent`; `/terms`+`/about` добавлены в `PRE_CONSENT_PATHS` (доступны до cookie-согласия, как `/privacy`). Теперь с любого публичного экрана (код/рега/вход/recover) можно вернуться на главную и дойти до доков (решает п.1–3, п.6). `ng build` зелёный.
  - [x] **F5.2** — ✅ правовые тексты (**черновики под ревью Elmir'а**): `/privacy` (Политика — что собираем/чего нет/cookie/152-ФЗ no-PII/хранение/права/без гарантий) + `/terms` (Условия — что это/правила/приглашения-модерация/«как есть» без гарантий + могут выключить/open-source self-host/прекращение). Cookie-баннер → ссылки на **оба** (`/terms` уже в `PRE_CONSENT_PATHS`). Стиль унифицирован (`legal-page.scss`, privacy переведён, старый scss удалён); layout `justify-content: safe center` (длинные доки не обрезаются сверху). `ng build` зелёный. **Тексты — рыба под твою правку, без юр-претензии.**
  - [x] **F5.3** — ✅ `/about` «О проекте» (по бест-практисам: «зачем» вперёд стека, сканируемость, «built with» с обоснованием, честность как фича). `AboutComponent` (данные-массивы): герой+миссия · принципы (сетка карточек) · **стек с «почему»** (Angular/Nest/Drizzle/Postgres/Docker+Traefik/SCSS — адаптивная grid) · **Claude Code + модели** (прозрачно) · open source (GitHub + Apache-2.0 + self-host + контрибьютинг) · честно о рисках (→ `/terms`) · контакты (**Telegram**). Раскладка: разведены ширины — формы/правовые узкие (440/680), about широкий (920, grid). **TODO-плейсхолдеры** (Telegram `@your_telegram`, repo URL) помечены `TODO: Claude Code` — Elmir пришлёт реальные. `ng build` зелёный.
  - [x] **F5.4** — ✅ репо-доки: `LICENSE` (Apache-2.0, полный текст) + `NOTICE` (первичный автор Elmir Kuba + прозрачность про Claude Code) + `CONTRIBUTING.md` (Apache-2.0 без CLA, атрибуция/вписать себя, локальный старт, конвенции кода, процесс PR, контакты) + `README.md` **переписан под факт** (был фаза-0: TypeORM/Tailwind/«кода нет/18 ADR» → теперь реальный стек Drizzle/чистый SCSS, что готово в фазе 1, быстрый старт, прозрачность об ИИ, лицензия).
  - [ ] **F5.5** — pre-deploy hygiene: 404-страница (public + `/app`), `noindex` + `robots.txt` (invite-only, не индексируем), favicon + `<title>Нормисы` + meta/OG (без трекинга), глобальная error/offline-граница, версия/коммит в футере. **🔒 F5.5·БЭК** — rate-limiting на auth (`login`/`register`/`recover`/`invite-check`) против брутфорса (обязателен перед публичным деплоем).
  - [ ] **F5.6** — центр уведомлений (п.8; разбить при старте на `F5.6·БЭК` + `F5.6`): бэк — таблица `notifications` + эндпоинты (список / отметить прочитанным) + системные «о новых релизах/обновлениях»; фронт — колокол в шапке + центр уведомлений. Полноценная фича (самая тяжёлая в F5).

### F6. Правовой анализ и финализация (ПЕРЕД D1)
- [ ] **F6** — pre-deploy правовой гейт. Elmir прогоняет тексты через юр-AI и присылает; Opus вставляет и делает **анализ+доводку**. **Сделано:** Политика конфиденциальности (`/privacy`) обновлена внешней (юр-AI) редакцией от 9 июня 2026 (комплаенснее: локализация в РФ, законные запросы, срок хранения, права). **Осталось при F6:**
  - [ ] **F6.1** — Условия использования (`/terms`). _Текст вставлен_ (внешняя юр-AI редакция от 9 июня 2026, 11 разделов; файл был в битой кодировке UTF-8→Latin-1 — декодирован; `ng build` зелёный), но **отметку не ставим — финальный проход по F6 после всех F5** (реш. Elmir 2026-06-09). Останется сверить с кодом/ADR в F6.2.
  - [ ] **F6.2** — анализ Opus'ом обоих текстов: внутренние противоречия; **соответствие тому, что код реально делает** (напр. «локализация в РФ» vs фактический хостинг; «удаляются/обезличиваются» vs soft-delete `deletedAt`; контакты — указать реальные в `/about`); согласованность с философией/ADR — **напряжение ADR-0023** («щит приватности — отсутствие ПДн, не локация»; переезд вне РФ под давлением) против пунктов про РФ-локализацию и раскрытие органам — вынести на решение Elmir'а. Спорное → ADR/правка. Итог: финальные согласованные тексты + (при необходимости) правка кода/ADR под заявленное.

### D. Деплой
- [ ] **D1** — прод-деплой «для своих» (`нормисы.рф`). Черновик уже есть: Traefik v3.3 + Let's Encrypt (TLS-ALPN, HTTP→HTTPS), оба `Dockerfile.production` (multi-stage), nginx SPA-fallback, postgres prod (internal, без портов наружу), volumes (pg_data_prod/letsencrypt/content), `db:migrate` (drizzle-kit) + миграция `0000_…`, make `prod-build/up/down`. **Решено разбить на подэтапы; пока только план, не реализуем.**
  - **Зафиксированное решение по роутингу (Elmir 2026-06-09):** **same-origin path-based** (НЕ поддомен). Код уже под него: `environment.prod.apiBase=''`, nginx-коммент. Черновик `compose.prod` сейчас противоречит (роутит API на `api.${DOMAIN}`) — это **баг черновика**, чиним: Traefik `Host(нормисы.рф)→angular`, `Host(нормисы.рф) && PathPrefix(/api)→nest` (приоритет). Бонус: ноль CORS, cookie тривиально, один домен/серт. **IDN** `нормисы.рф`→punycode (`xn--…`) в Host-правилах/ACME.
  - [ ] **D1.1** — свести прод-конфиг к path-based + прод-поправки кода: Traefik-лейблы (path-based вместо поддомена); `CORS` config-driven/выкл при same-origin (сейчас хардкод `localhost` в `main.ts`); `trust proxy` (Express за Traefik — Secure-cookie/`req.secure`/реальный IP); `COOKIE_SECURE=true`; `.env.prod.example`. Проверка: `make prod-config`.
  - [ ] **D1.2** — миграции в прод: one-shot migrate (`drizzle-kit migrate`) ОТДЕЛЬНЫМ шагом до старта nest (auto-push запрещён, ADR-0030) — compose-сервис или make-шаг; прогон на чистой prod-БД.
  - [ ] **D1.3** — локальный прогон всего прод-стека: `prod-build` + `up` локально, один origin (SPA + `/api`), аватары через content-volume, сертификат (LE-staging/self-signed). Smoke: рега/вход/аватар/приглашения по HTTPS локально.
  - [ ] **D1.4** — VPS + DNS + боевой деплой (нужен сервер+домен): провижн (Docker), A-запись `нормисы.рф`→IP, секреты в `.env` на сервере (JWT/пароль БД/`DOMAIN`/`ACME_EMAIL`), `prod up`, LE-серт выписался, прод по HTTPS, smoke.
  - [ ] **D1.5** — ops + runbook: firewall (22/80/443), бэкап pg (cron `pg_dump`), make `prod-deploy`/`prod-logs`, `docs/deployment.md` приведён к факту.

## Принципы кодинга (из доки)
- **Типы — иерархией** `Pure→Base→Full` + производные утилитами (`Pick`/`Omit`/`Required`/`Partial`), классы через `implements`; одно свойство — одно место; per-project (не шарим фронт↔бэк) ([ADR-0033](./decisions/0033-type-hierarchy-convention.md)). Применяется со S1/A1.
- 5-слойка строго; кросс-домен только вниз (use-case→чужой domain-service).
- Бизнес-слои не импортируют Drizzle. Конфиг только через ConfigService. Без `any`.
- Каждый завершённый этап: сборка проходит → коммит → отметка здесь.
- Один коммит = один слой/одна фича (не мешать nest+angular в одном).

## Заметки/риски
- nest и angular — свои package.json (монорепо без workspace, как договорено).
- Пароль 3–64 (осознанный риск, ADR-0032).
- Аватар — фронт режет, бэк принимает готовое (ADR-0032).
