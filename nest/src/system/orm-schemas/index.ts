// Drizzle-схемы таблиц появятся на этапе S1 (accounts, secret_qa, invite_codes,
// invitations, bans, sessions — ADR-0032). Пока пусто: файл существует как точка
// сборки схемы для drizzle.config.ts и барреля репозиториев.
export {};
