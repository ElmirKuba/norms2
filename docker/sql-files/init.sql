-- Инициализация PostgreSQL для «Нормисов».
--
-- КОГДА: автоматически и ТОЛЬКО при ПЕРВОМ старте контейнера на пустом volume
-- (из docker-entrypoint-initdb.d), ДО старта приложения, один раз за жизнь тома.
-- На существующих данных НЕ запускается. Поэтому сюда — НЕ схему и НЕ то, что
-- должно применяться на каждом деплое.
--
-- ЧТО СЮДА: только пред-миграционные пререквизиты уровня БД (расширения, роли),
-- которые должны существовать ДО миграций.
--
-- СХЕМУ ведёт Drizzle-kit миграциями (ADR-0030) ОТДЕЛЬНЫМ шагом `drizzle-kit
-- migrate` на КАЖДОМ деплое (идемпотентно), а НЕ здесь. Порядок на чистой БД:
--   init.sql  ->  drizzle-kit migrate  ->  старт nest (+ сид релиз-нот, F7).
--
-- СЕЙЧАС пререквизитов НЕТ (файл — пустой плейсхолдер):
--  * PK/FK — строки uuidv7___unixmillis от приложения (generateId, ADR-0016),
--    не из БД -> UUID-расширения не нужны;
--  * регистронезависимый login — функциональным unique-индексом lower(login)
--    (в миграции) + нормализацией в Login VO, без citext.
--
-- Добавлять CREATE EXTENSION / CREATE ROLE сюда по мере необходимости.
