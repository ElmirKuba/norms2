-- Инициализация PostgreSQL для «Нормисов».
-- Выполняется автоматически при ПЕРВОМ старте контейнера (пустой volume),
-- из каталога docker-entrypoint-initdb.d. На существующих данных не запускается.
--
-- Идентификаторы (PK/FK) — строки формата uuidv7___unixmillis, генерируются
-- приложением (util generateId()), НЕ в БД (ADR-0016). Поэтому расширения для
-- генерации UUID тут не нужны. Схему таблиц ведёт Drizzle-kit миграциями
-- (ADR-0030), здесь — только то, что должно существовать до миграций.

-- citext: регистронезависимые текстовые сравнения (например, для login).
CREATE EXTENSION IF NOT EXISTS citext;
