/**
 * Ответ `GET /invites/can-ban/:accountId` — вправе ли смотрящий забанить этот
 * аккаунт (цель ≠ сам И в своём поддереве). Для видимости кнопки бана на карточке
 * (F3.Д): не показывать действие, на которое нет права.
 */
export interface CanBanResponse {
  /** true, если бан этого аккаунта разрешён текущему пользователю. */
  allowed: boolean;
}
