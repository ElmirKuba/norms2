/** Кто совершает административное действие: нужен журналу (2.9.3). */
export interface AdminActor {
  /** Аккаунт админа. */
  accountId: string;
  /** Логин админа — снимком в журнал: след должен пережить удаление аккаунта. */
  login: string;
}
