# Контекст сборки образов — корень репозитория. Исключаем тяжёлое и лишнее,
# чтобы контекст не раздувался и кэш слоёв не инвалидировался зря.
**/node_modules
**/dist
**/.angular
**/coverage
**/*.tsbuildinfo
.git
docker/volumes
**/*.log
.env
.env.*
!.env.example
