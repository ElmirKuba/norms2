/**
 * Опаковый дескриптор транзакции. Домен получает его от `TransactionRunner` и
 * передаёт в методы портов, НЕ зная, что внутри (Drizzle-tx). Так транзакция
 * оркестрируется в домене без утечки ORM. Конкретику знает только инфра.
 */
export interface Transaction {
  /** Внутренний маркер; напрямую не используется. */
  readonly __transaction: true;
}
