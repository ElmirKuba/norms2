/**
 * Роли, которые сид гарантирует на старте (2.9.3·2).
 *
 * **Справочник, а не enum в коде.** Новая роль добавляется строкой здесь; таблица `roles`
 * заполняется идемпотентно по `code` (уникальность без учёта регистра делает база).
 *
 * ⚠️ **`code` — это контракт с кодом проверки прав.** Переименование существующего кода молча
 * разъедет проверки: guard будет искать роль, которой больше нет, и все потеряют права. Меняем
 * только `title` и `description`; новый смысл — новая строка.
 */
export const ROLES_SEED: readonly { code: string; title: string; description: string | null }[] = [
  {
    code: 'user',
    title: 'Пользователь',
    description:
      'Базовая роль: есть у каждого аккаунта. Отдельной строкой, а не «по умолчанию», ' +
      'потому что человек может быть одновременно пользователем и админом (реш. Elmir 09.08.2026).',
  },
  {
    code: 'admin',
    title: 'Администратор',
    description:
      'Техническая админка и права в Telegram-боте: привязал Telegram человек с этой ролью — ' +
      'бот слушается его как админа (2.9.3·3а).',
  },
];

/** Код базовой роли, которая должна быть у каждого аккаунта. */
export const ROLE_USER = 'user';
/** Код административной роли. */
export const ROLE_ADMIN = 'admin';
