/**
 * BanPure — содержательное поле бана (ADR-0033), без id/FK/системных меток.
 * Нижний слой иерархии типов.
 */
export interface BanPure {
  /** Причина бана (свободный текст; предупреждение про ПДн в UI — ADR-0001). */
  reason: string;
}
