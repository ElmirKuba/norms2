-- Выпуск переименован в 2.9.3 (реш. Elmir 14.08.2026).
--
-- Номер поднялся ещё в `12540cb` — вместе с лендингом, — а публикация в базе осталась под старым
-- ключом. Ключ у нас и есть публичный адрес, поэтому расхождение было видно людям: портал
-- искался по `release-2.9.3`, а строка называлась `release-2.9.2`, и страница выпуска
-- открывалась пустой. Правим базу под код, а не наоборот: адрес `/releases/release-2.9.2`
-- перестанет существовать (старая ссылка из канала — цена решения, принята осознанно).
UPDATE "releases"
SET "key" = 'release-2.9.3',
    "title" = replace("title", '2.9.2', '2.9.3')
WHERE "key" = 'release-2.9.2';
