# phase2-analysis-map.md — карта анализа и синтеза Фазы 2 «Акцент»

> **🎯 Назначение.** Resumable-план: как переварить источники вдохновения в **свою** доку Фазы 2, не теряя контекст между сессиями (лимиты ограничены). По образцу `archive/decision-map.md`.
>
> **Правило (жёсткое, [CLAUDE.md](../CLAUDE.md) п.5, [Вдохновиться/README.md](../Вдохновиться/README.md)):** из источников берём **только идеи**. НИКОГДА не копируем схемы БД, DTO, миграции, API-контракты, формулировки. «Акцент» у нас — **раздел внутри Нормисов** на общем ЛК (accounts, id `uuidv7___unixmillis`, слоистый Nest, SCSS+MatDialog), а не самостоятельный продукт. Всё переписываем с нуля под фундамент фазы 1.
>
> **Финальное состояние (цель Elmir):** в `Вдохновиться/акцент/` не остаётся файлов И удалён `~/Downloads/Telegram Desktop/superbetter_life_os_spec_ru.md`. Удаление источника — **только после ✅ digested** (идея захвачена в Леджер §5).

---

## 0. Видение Фазы 2 (захвачено нашими словами — НЕ потеряется, даже если все источники удалить)

«Акцент» — **личная ОС достижений** в духе SuperBetter + Tiny Habits (BJ Fogg). Ядро:

1. **Сверх-низкий порог входа.** 1 отжимание = засчитанная тренировка. Цель не «100 отжиманий», а «сделай 1, остальное бонус». Импульс важнее объёма.
2. **Лесенка прогресса.** Сегодня 1, завтра 2, через 11 дней 10+1 — система ведёт по микро-шагам к крупной цели, подбирая следующий чуть-больший шаг.
3. **Всё засчитывается.** Даже стакан воды → победа/очко. Любое микро-действие можно зафиксировать и получить признание.
4. **Геймификация:** очки, серии (streaks), достижения, уровни — как поддержка мотивации, не казино.
5. **Целеполагание под контролем:** цели → задачи → привычки/анти-привычки, с серверной агрегацией прогресса и идемпотентностью отметок.
6. **Круг доверия = инвайт-дерево Нормисов.** «Три близких, кому доверяешь» — маленький круг подотчётности. В фазе 2 — соц-фичи осторожно/опционально.

**North star:** даже демотивированный человек, делая смешно-маленькие шаги, забирает контроль над жизнью. Бесплатно, opensource, «для своих».

> Приоритетный артефакт против потери контекста. Если сессия оборвётся — Фаза 2 стартует отсюда.

---

## 1. Инвентарь источников (ПРОВЕРЕНО: дубликатов нет, все md5 уникальны)

### `Вдохновиться/акцент/` — 14 .md + 2 в mnt
| Файл | Строк | Приоритет | Зачем |
|---|---|---|---|
| `gamification.md` | 166 | 🔴 HIGH | ядро видения: очки/серии/достижения/лесенка |
| `Technical-assignment.md` | 301 | 🔴 HIGH | продуктовый scope «Акцента» (идеи фич) |
| `domain-model.md` | 325 | 🔴 HIGH | доменные понятия для переформулирования |
| `ui-ux.md` | 310 | 🟠 MED | UX-сценарии для адаптации (не копировать вёрстку) |
| `README.md` | 31 | 🟠 MED | краткий обзор/контекст |
| `todo.md` | 302 | 🟢 LOW | их план работ — нам не нужен (есть фичи-намёки) |
| `claude.md` | 137 | 🟢 LOW | их рабочий процесс — у нас свой |
| `database.md` | 309 | ⚪ SKIP* | своя схема с нуля; глянуть лишь перечень сущностей |
| `api-contracts.md` | 511 | ⚪ SKIP* | свои контракты с нуля; глянуть лишь список действий |
| `architecture.md` | 187 | ⚪ SKIP | стек/слои уже свои (фаза 1) |
| `backend.md` | 270 | ⚪ SKIP | то же |
| `frontend.md` | 244 | ⚪ SKIP | то же |
| `deployment.md` | 268 | ⚪ SKIP | то же |
| `getting-started.md` | 231 | ⚪ SKIP | то же |
| `mnt/.../ascent/angular/README.md` | — | ⚪ SKIP | артефакт сборки |
| `mnt/.../ascent/nest/README.md` | — | ⚪ SKIP | артефакт сборки |

\* SKIP-с-беглым-сканом: открыть только ради списка сущностей/действий, идеи → Леджер, потом удалить.

### Внешний источник (вторая очередь)
| Файл | Строк | Приоритет | Когда |
|---|---|---|---|
| `~/Downloads/Telegram Desktop/superbetter_life_os_spec_ru.md` | 932 | 🔴 HIGH | после акцента; «усилитель видения» (SuperBetter life-OS) |

**Объём:** акцент ~208 KB, спек ~36 KB. Суммарно ~3.5к строк акцента + ~0.9к спека. → читать батчами, не разом.

---

## 2. Протокол переваривания (на каждый источник)

1. **Read** файл (по приоритету).
2. **Извлечь идеи** в Леджер (§5) — своими словами, без копирования (только механики/UX-сценарии).
3. **Отметить** статус в §4 (📖) + куда легли идеи.
4. **Удалить** источник (`rm`) — только после п.2–3 **и** после контрольной проверки, что bash отдаёт корректный вывод (см. ⚠️ ниже).
5. После всех источников — **синтез** Леджера в reference-доки Фазы 2 (§6) + ADR на развилки (нумерация 0027+).

**Анти-копирование:** соблазн взять схему/DTO/миграцию/формулировку дословно → СТОП, переформулировать или спросить Elmir.

⚠️ **Про bash:** в сессии создания карты shell иногда давал обрезанный/искажённый вывод (поэтому изначально привиделись «дубликаты»). Перед любым `rm` — контрольный `ls`/`wc -l` известного файла; если вывод странный, удаление отложить.

---

## 3. Батчи (каждый самодостаточен, начинать с чтения §0 + «Текущая позиция»)

- **Батч A (✅ done):** разведка + эта карта. Удалений нет.
- **Батч B:** `gamification.md` → Леджер → удалить.
- **Батч C:** `Technical-assignment.md` → Леджер → удалить.
- **Батч D:** `domain-model.md` + `README.md` → Леджер → удалить.
- **Батч E:** `ui-ux.md` + беглый скан `database.md`/`api-contracts.md` (только сущности/действия) → Леджер → удалить эти + остальные SKIP (`architecture/backend/frontend/deployment/getting-started/claude/todo/mnt`).
- **Батч F:** `superbetter_life_os_spec_ru.md` → Леджер → удалить.
- **Батч G (синтез):** Леджер → `docs/sections/accent/*` (§6) + ADR Фазы 2. Обновить TA/todo/CLAUDE/decisions-index.
- **Батч H (финал):** убедиться, что `Вдохновиться/акцент` пуста и спец-файл удалён; проверка непротиворечивости; коммит/пуш.

---

## 4. Статус источников (обновлять каждый батч)
Легенда: ⬜ не тронут · 📖 прочитан/в Леджере · 🗑 удалён

| Источник | Статус | Идеи легли в |
|---|---|---|
| `gamification.md` | 📖→🗑 | §5 Механики геймификации |
| `Technical-assignment.md` | 📖→🗑 | §5 Цели/задачи + Лесенка + развилки R1-R5 |
| `domain-model.md` | 📖→🗑 | §5 Доменные сущности (детальные поля) + R6/R7 |
| `ui-ux.md` | 📖→🗑 | §5 UX-сценарии (тон «интерес не страх», якорь, мин.день) |
| `api-contracts.md` | 📖(скан)→🗑 | §5 (cursor-пагинация; контракты пишем сами) |
| `database.md` | 🗑(SKIP) | схему пишем с нуля; сущности уже в §5 из domain-model |
| `architecture.md` | 🗑(SKIP) | стек/слои свои (фаза 1) |
| `backend.md` | 🗑(SKIP) | свои конвенции |
| `frontend.md` | 🗑(SKIP) | свои конвенции |
| `deployment.md` | 🗑(SKIP) | своё |
| `getting-started.md` | 🗑(SKIP) | своё |
| `claude.md` | 🗑(SKIP) | их рабочий процесс не нужен |
| `todo.md` | 🗑(SKIP) | их план не нужен |
| `mnt/.../angular/README.md` | 🗑(SKIP) | артефакт сборки |
| `mnt/.../nest/README.md` | 🗑(SKIP) | артефакт сборки |
| `README.md` | 📖→🗑 | §0/§5 (обзор фич — дублирует TA, ничего нового) |
| `superbetter_life_os_spec_ru.md` (внешний) | ⬜ | батч F |

**Папка `Вдохновиться/акцент` — ПУСТА** (все источники переварены и удалены, проверено через `git ls-files` = 0). Остался только внешний спек.

**Добор для полноты (по просьбе Elmir «ничего не упустить»):** прочитаны оригиналы `database.md` + `todo.md` из `~/Downloads/files/` (НЕ удаляются — это бэкап Elmir). Все детали-крохи (вычисляемые поля целей, паузы, ролловер, edge-cases, вычисл. поля Task, dashboard-агрегатор, R8/R9) — в §5. Акцент переварен на 100%.

---

## 5. Леджер идей (растёт по мере чтения — «память» между сессиями)

### Механики геймификации
_(из `gamification.md` — идеи, переформулированы; конкретные числа берём как стартовые наброски, перетюним под наше видение «всё засчитывается»)_
- **Геймификация поверх, не вместо.** Очки — отражение прогресса, НЕ валюта: нельзя потратить, нет магазина, нет pay-to-win, нет gate привилегий. Ачивки за осмысленное, не за гринд. (совпадает с нашим «не казино»)
- **XP за действия** (старт-наброски): задача 10; задача из цели +5; привычка/день 8; день без анти-привычки 8; дневник дня 5; серия 7д +20; серия 30д +100; цель 100% → 200 разово.
- **Уровни** = от суммарного XP, формула `level = floor(sqrt(totalXP/100))+1` (0/100/400/900/1600…). Чисто визуальный статус, без привилегий.
- **Серии (streaks):** последовательные дни; пропуск → сброс в 0; отображение 🔥N на карточке. **Freeze:** 1 заморозка/неделю спасает серию, не накапливается (use-it-or-lose-it). ⟶ хорошо ложится на «не наказывать за срыв».
- **Достижения** (примеры идей): Первый шаг (1-я задача), Достигатор (1-я цель), Неделя силы (7д), Железная воля (30д), Мастер привычек (5 привычек × 14д), Победитель (анти-привычка 30д), **Возвращение (вернуться после обрыва 7+д)** ← важно для «опущенного Васи», Сова/Жаворонок (время суток).
- **Анти-читинг (ВАЖНО, ложится на наши принципы):** XP только сервером; идемпотентность отметки (повтор не начисляет); дневной лимит источников (дневник 1/день); бэкдейтинг фиксирует факт, но XP не даёт. ⟶ согласуется с фазой-1: серверная агрегация + идемпотентность.
- **Модуль `gamification` подписан на доменные события** других модулей и начисляет награды; не дублирует прогресс, а читает события. ⟶ архитектурная идея: event-driven, отдельный модуль в нашей слоистой структуре.

> ⚠️ Файл `gamification.md` оказался усечён (строки 70–79 — повтор «Связь с другими модулями» + плейсхолдеры «усечено»). Раздел про связь с модулями в источнике неполный — учтём при синтезе (свяжем с goals/tasks/habits сами).

### Цели / задачи / привычки / анти-привычки
_(из `Technical-assignment.md` акцента — идеи переформулированы; их «фазы 0-9» и стек игнорируем, у нас свой фундамент)_
- **Состав сущностей-кандидатов:** `Goal` (большая измеримая цель), `WeeklyGoal` (недельный фокус), `Task` + `TaskTemplate` (повтор по RRULE), `AntiHabit` (таймер «держусь»), `Workout` (упражнения→подходы→повторы), `PointEvent` (идемпотентный факт очков), `Achievement`, `DayLog` (снимок дня: задачи/%/дневник).
- **Goal:** title, категория (напр. spiritual/physical/mental — у нас категории сделаем настраиваемыми, не навязанными), unit (км/раз/страницы), targetValue/currentValue, deadline. Прогресс = current/target. Записи прогресса = `GoalEntry` (дата, значение, заметка).
- **Task↔Goal:** выполнение задачи, привязанной к цели, даёт прогресс цели. Приоритет low/normal/high влияет на очки.
- **TaskTemplate = RRULE** (пн/ср/пт и т.п.); конкретные `Task` материализуются планировщиком на дату. День без материализации по RRULE серию НЕ сбрасывает.
- **AntiHabit = timer-модель** (не счётчик отметок): держусь N дней непрерывно; «Рецидив» сбрасывает таймер и растит attemptNumber; рекорд обновляется при рецидиве. Milestone-очки на 3/7/14/30… через отложенные джобы (у нас — `@nestjs/schedule`/очередь, без BullMQ если можно проще).
- **WeeklyGoal:** промежуточный горизонт между днём и большой целью, сбрасывается в понедельник.
- **DayLog:** снимок дня — основа серий и недельных сводок; % выполнения дня.
- **Workout:** структура упражнение→подход(вес,повторы); история для прогрессии весов; завершённая = очки. ⟶ кандидат отложить на под-фазу (сложнее остального).

### Прогресс-лесенка и микро-шаги
- **Конкретика вместо размытости:** каждая цель измерима, каждая задача выполнима «за один тап» (≈30 сек отметить). Это и есть техническая опора нашего «1 отжимание = победа».
- **Восстановление после срыва (comeback) — продуктовый принцип №1 для нашего «Васи»:** пропуск помогает вернуться, а не штрафует деморализующе. Метрика успеха = «после срыва возвращается, а не бросает».
- **Чем НЕ todo-list:** todo показывает невыполненное (чувство вины), «Акцент» показывает прогресс/рост (эмоция). Серии создают инерцию «не хочу терять streak».
- _(механику авто-подбора следующего чуть-большего шага — 1→2→…→100 — источник явно НЕ описывает; это НАШЕ усиление, спроектируем сами в синтезе → развилка ниже)_

### Открытые развилки Фазы 2 (→ ADR в батче G)
- **R1 Категории целей:** навязанные (spiritual/physical/mental) vs свободные пользовательские? (склоняемся к свободным + пресеты).
- **R2 Авто-лесенка прогрессии:** как система подбирает следующий шаг (1→2→10+1)? Линейно/адаптивно/вручную? — ядро нашего видения, источник не покрывает.
- **R3 Workout-модуль:** в scope фазы 2 или отложить как под-фазу 2.x (он самый тяжёлый).
- **R4 Realtime:** источник хочет WebSocket для мгновенных очков. Нужен ли нам в фазе 2 или хватит оптимистичного UI + REST? (склоняемся ко второму на старте).
- **R5 Соц/круг доверия:** источник = строго одиночный (юзеры не видят друг друга). Наше видение хочет «круг доверия» через инвайт-дерево. Развилка: оставить одиночным в фазе 2, соц — отдельной под-фазой?
- **R6 timezone/locale на аккаунте:** у Акцента всё завязано на TZ юзера (ролловер дня, серии по локальной полуночи) и locale (RU/EN). В нашем `accounts` (фаза 1) этих полей НЕТ. Развилка: добавить `timezone`/`locale` в accounts, или хранить в настройках раздела? (locale у нас = RU по [ADR-0022](./decisions/0022-concept-and-philosophy.md), но TZ для ролловера нужен реально).
- **R7 Состав MVP фазы 2:** источник тянет 10 под-модулей (goals/tasks/anti-habits/weekly/metrics/lessons/workouts/scheduled/gamification/dashboard). Для нашего MVP «Акцента» — что в первую очередь, что отложить? (кандидат-ядро: goals + tasks + gamification + лесенка; остальное волнами).

### Доменные сущности — детальные поля (из `domain-model.md`, переформулировано; uuid/email/bcrypt → у НАС: id `uuidv7___unixmillis`, аккаунт уже есть в ЛК фазы 1, не дублируем User)
_Важно: их `User` = наш `accounts` из фазы 1. НЕ заводим заново — все сущности Акцента вешаем на `account_id`. timezone/locale: в фазе 1 их у accounts нет → развилка R6._
- **Goal:** account_id, title, category, unit (свободная строка: «км/раз/страниц/дней»), targetValue, currentValue (кэш = Σ entries), deadline?, status (active/completed/archived), completedAt?. Инвариант: current≥target → completed (не удаляется); target>0.
- **GoalEntry:** goalId, value (можно отрицательное — коррекция, ≠0), note?, occurredOn (дата в TZ юзера), createdAt.
- **TaskTemplate:** account_id, title, description?, icon?, priority (low/normal/high → влияет на очки), recurrence (RRULE-объект: freq/byweekday/interval), active.
- **Task:** templateId? (null=разовая), account_id, title, date, status (pending/done/skipped), completedAt?, skipReason? (postponed/cancelled). Уник (templateId,date) — 1 инстанс шаблона на день.
- **AntiHabit (timer):** account_id, title, description?, startedAt, attemptNumber, longestStreakDays. Серия=floor((now−startedAt)/сутки); рецидив→обновить рекорд, startedAt=now, attempt++.
- **AntiHabitDay** (опц., история): antiHabitId, date, state (held/relapsed).
- **WeeklyGoal** + **WeeklyGoalItem:** недельный фокус (weekStart=пн) + чек-айтемы (text, done).
- **DailyMetric:** account_id, date (уник с account), sleepHours? (0..24, шаг .5), energy? (1..10), mood? (1..10), note?.
- **DailyLesson:** account_id, date, content (markdown), tags[].
- **Workout/Exercise/WorkoutSession/SetLog:** шаблон трен.→упражнения(targetSets/Reps,lastWeight,order); сессия→подходы(setNumber,reps,weight,rpe?). [тяжёлый модуль — R3].
- **ScheduledAction:** account_id, title, dueDate, remindDaysBefore?, status(open/done) — разовое дело с напоминанием.
- **PointEvent:** account_id, source(enum), sourceId(строка), points, occurredOn, revokedAt?. Уник (source,sourceId) среди не-revoked = идемпотентность.
- **Achievement** (каталог: code PK, title/desc/icon) + **UserAchievement** (account_id, code, awardedAt; уник (account,code)).
- **Доменные правила (сводка):** агрегаты на бэке; идемпотентность через unique; даты в TZ юзера; историю не править задним числом; мягкое удаление через status=archived.

### UX-сценарии (из `ui-ux.md`, переформулировано; их палитра/Tailwind НЕ берём — у нас своё ([ADR-0025](./decisions/0025-ui-ux-design-language.md): тёмная+toggle, SCSS, MatDialog))
- **Дашборд = «личный кокпит»** — всё важное на одном экране сверху вниз: хедер (уровень+XP-бар), «Сегодня» (задачи+donut % дня), активные цели (3-5 карточек progress-bar), серии (🔥N), быстрый ввод метрик, просроченные действия. ⟶ ядро UX, ложится на наше «прогресс виден сразу».
- **Экраны-кандидаты:** `/` дашборд, `/goals` + `/goals/:id` (donut, line-chart прогресса, прогноз «успеешь к дате»), `/tasks` (вкладки Сегодня/Шаблоны, свайп→перенос), `/anti-habits` (большой таймер «N дней без срыва», кнопка «Сорвался»+подтв.), `/weekly`, `/progress` (heatmap как GitHub, сетка достижений locked/unlocked), `/profile` (уровень/XP/настройки).
- **Принципы UX:** один primary-акцент на экран; мгновенный отклик (toast «+N XP», анимация чекбокса); mobile-first (нам важно — фаза 6 Capacitor); 4 состояния каждого экрана (loading=скелетоны, empty=призыв «Создай первую цель», error+повтор, loaded).
- **Переиспользуемые компоненты-кандидаты** (наши, на SCSS): ProgressDonut(SVG), ProgressBar, StreakBadge(🔥+N), XpBar, TaskCard, GoalCard, MetricSlider(1..10), Heatmap, EmptyState, Toast. Modal — через MatDialog ([ADR-0026](./decisions/0026-modal-system.md)).
- **Геймификация в UI:** XP-бар всегда в хедере; toast на каждое действие; достижение = модалка с конфетти (CSS, без либ); 🔥 везде где streak; уровень крупно. ⟶ «не крикливо» (совпадает с нашим «не казино»).
- **a11y:** prefers-reduced-motion отключает анимации; ARIA на кастомных; контраст ≥4.5 — взять как требование.
- _Пустые состояния с призывом — прямой союзник «1 отжимание»: empty не вина, а приглашение сделать микро-шаг._
- **🔑 ТОН UI (золото из ui-ux, прямо наше видение):** «интерес, не страх». Каждый текст двигает вперёд, не давит. Срыв → «Comeback начинается сегодня» (не «серия прервана»); задача не сделана → «серия ждёт продолжения»; пустой экран → «день начинается — первый шаг?»; срыв анти-привычки → «новая попытка». Достижения **частые в начале** (первая задача/неделя/уровень), редкие позже — имитируют кривую нарастающего интереса до того, как мотивация включилась.
- **«Минимум плохого дня» (= наш `minVersion`/лесенка вживую):** вечером, если задача не сделана и серия под угрозой — карточка показывает мини-версию («Минимум сегодня: 5 мин растяжки»). Данные уже в задаче, без доп.запроса. ⟶ прямой кирпич для R2.
- **«Урок дня как якорь»:** фраза, которую юзер написал себе сам из ресурсного состояния, показывается когда он не в ресурсе (открыл вечером вместо YouTube). Не цитата из интернета — твои слова тебе. Сильная антирецидивная механика.
- **Anti-habit экран:** большой живой таймер дн:ч:мин:сек (фронт `setInterval`), circular ring = currentDays/targetDays, «Рецидив» с подтверждением+заметкой, история попыток.

### Беглый скан `api-contracts.md` + `database.md` (только идеи, контракты пишем С НУЛЯ под наш стек)
- Их формат ошибок/auth/пагинация — НЕ берём (у нас свой конверт [ADR-0020], свой auth фазы 1). Взять как идею: **cursor-пагинация** для потенциально длинных списков (goal-entries, история) — у нас в фазе 1 пагинации не было, для Акцента может понадобиться → мелкая развилка при синтезе.
- Идемпотентность отметок — через идемпотентные эндпоинты complete/uncomplete (совпадает с PointEvent unique).
- Полные REST-контракты Акцента спроектируем сами в батче G/слой1 на базе наших сущностей (§5).

### Добор `database.md` (из ~/Downloads/files — только полезные индексы/инварианты, схему пишем сами под наш id `uuidv7___unixmillis`)
- **Индексы-кандидаты (взять при проектировании нашей схемы):** goals `(account,status)`+`(account,category)`; goal_entries `(goal,date)`; tasks `(account,date)` + UNIQUE `(template,date) WHERE template NOT NULL` (1 инстанс шаблона/день); weekly UNIQUE `(account,week_start,position)`; daily_metrics/daily_lessons UNIQUE `(account,date)`; lessons GIN на `tags[]`; achievements UNIQUE `(account,code)`.
- **PointEvent идемпотентность = частичный UNIQUE** `(source, source_id) WHERE revoked_at IS NULL` — изящно: revoke→можно начислить заново. ← берём этот приём.
- **FK-политика:** зависимые → `ON DELETE CASCADE` (goal_entry→goal), опасные → `RESTRICT`; template→task `ON DELETE SET NULL` (история задач не рушится). У нас accounts soft-delete ([ADR-0017]) → каскадов на удаление аккаунта не будет, но внутри Акцента (goal→entries) каскад уместен.
- **Числа прогресса — `numeric`, не float** (точность). ← берём.
- **goal pause:** `paused_at` + `pause_history jsonb [{pausedAt,resumedAt}]`, пауза не принимает entries, в дашборде «на паузе». ← хорошая идея (была неявно в domain, теперь явно).
- их users/refresh_tokens — НЕ берём (у нас accounts + sessions из фазы 1).

### Добор `todo.md` (детали реализации, которых не было в TA/domain — забираем, чтобы НИЧЕГО не упустить)
- **Goal вычисляемые поля (на чтение):** `currentValue` (Σ entries), `percentage` (min(cur/target*100,100)), `daysLeft`, `activeDays` (дни минус время на паузах), `pace` (cur/activeDays), `forecast` (ahead/on_track/behind по темпу vs дедлайн). ⟶ «успеешь ли к дате» = мотивирующий прогноз.
- **Goal пауза (полноценная фича, не только статус):** `POST pause`/`resume`; на паузе entries не принимаются (400); `activeDays`/`forecast` исключают паузы; в дашборде метка «на паузе»; история entries не теряется. Новый статус `paused` (в дополнение к active/completed/archived).
- **Task — расширенные поля для РАЗОВЫХ задач** (не только привычки): `category` (personal/work/health/finance/other), собственный `deadline`, `priority` до `urgent`, `overdue` статус (вычисляемый: pending+deadline<today). Привычки категорию не имеют — она от Goal.
- **Task действия:** complete (идемпотентно)/uncomplete/postpone(→завтра, текущий skipped+postponedFrom)/разовая. Эндпоинты `/tasks/overdue`, `/tasks/due-today`.
- **Ролловер задач:** материализация Task из TaskTemplate по RRULE в локальную полночь юзера (нужен TZ → R6). Edge-cases для тестов: DST, переход месяца. У нас вместо BullMQ-cron — `@nestjs/schedule` (или очередь), решим в синтезе.
- **Weekly-stats эндпоинт:** сводка недели = лучший день/худший день/средний %/total tasks done.
- **Dashboard = агрегирующий эндпоинт** `GET /dashboard` — всё за 1 запрос (серверная агрегация, наш принцип). Виджеты: сегодня/неделя/цели/метрики/просрочено.
- **Metrics-trends:** графики динамики сон/энергия/настроение за 7/30/90 дней.
- **Их 4 открытых вопроса (учесть в наших развилках):** (1) refresh opaque-в-БД vs JWT-jti → у нас уже решено: sessions в БД ([ADR-0018]); (2) серии материализовать vs считать на лету (window function) → решить по объёму, **R8**; (3) web-push для напоминаний vs in-app → **R9** (у нас email нет, напоминания только in-app/push); (4) Capacitor влияет на хранение токенов → у нас уже httpOnly cookie ([ADR-0020]), для нативных пересмотреть в фазе 6.
- **MVP-критерий (их):** рега→цель→инкремент→привычка в чеклисте→отметка→метрики→дашборд→1 достижение. Нам — ориентир для R7 (ядро MVP Акцента).

### Новые развилки из добора
- **R8 Серии:** считать on-demand (window function) или материализовать (`streak_state` + обновление)? Старт — on-demand, материализация при росте данных.
- **R9 Напоминания:** in-app vs web-push (email у нас нет). Старт — in-app; push — отдельной волной.

### Социальное / круг доверия (связь с инвайт-деревом)
- _(пусто — батч F, superbetter)_

### Идеи из SuperBetter-спека
- _(пусто — батч F)_

> Развилки R1–R7 — см. секцию «Открытые развилки Фазы 2» выше.

---

## 5.1 Два слоя синтеза (важно — мысль Elmir)

- **Слой 1 (батч G):** свести всё вдохновение в нашу доку Фазы 2 — переваренное, под наш фундамент, но ещё «по мотивам Ascent». Тут принимаем R1–R7.
- **Слой 2 (после G, отдельно):** доработать до **истинно нашей** доки — где НАШИ усиления (R2 авто-лесенка «1→2→…→100», R5 круг доверия через инвайт-дерево) выходят на первый план, а не просто адаптация. Не остановиться на «адаптации».
- Развилки (особенно R2) решаем **в синтезе с полной копилкой**, НЕ угадывая сейчас вслепую — в источниках может быть концепт прогрессии (проверить в `ui-ux.md`/superbetter-спеке на батчах E/F).

## 6. Целевая структура доки Фазы 2 (финализируем в батче G)

Кандидат (раздел по образцу фазы 1): `docs/sections/accent/` →
`README.md` (scope), `domain-model.md`, `gamification.md` (правила очков/серий/лесенки), `api-contracts.md`, `ui-ux.md` + ADR (0027+).
Привязка к фазе 1: всё на `accounts`/общем ЛК, id-формат, слоистый Nest, SCSS+MatDialog.

---

## Текущая позиция
- **Батч A ✅, B ✅, C ✅, D ✅:** переварены gamification, Technical-assignment, domain-model, README → §5, удалены. Осталось **10** .md в акценте + внешний спек.
- **Следующий — Батч E:** `ui-ux.md` (310) + беглый скан `database.md`/`api-contracts.md` (только сущности/действия) → §5 → удалить эти + остальные SKIP (architecture/backend/frontend/deployment/getting-started/claude/todo + mnt/).
- Зафиксированы развилки **R1–R7** для синтеза (батч G). Двигаться батчами; при <10% лимита — стоп, «Текущую позицию» держать точной.
- Файл-карта лежит в `docs/` (активный артефакт фазы 2); в archive уедет по завершении (как decision-map).
