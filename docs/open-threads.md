# open-threads.md — открытые хвосты

> **Что это.** Один список всего, что осознанно отложено или не закрыто, — чтобы не держать это
> в голове и не терять между заходами. Заведён 31.07.2026, после того как хвосты 2.7.x расползлись
> по трём файлам и жили только в переписке.
>
> **Правило ведения:** здесь **одна строка и ссылка**, подробности — в файле-хозяине. Дублировать
> содержание нельзя: разъедется. Закрыл хвост — удаляешь строку отсюда (не помечаешь ✅: список
> должен оставаться коротким, иначе его перестанут читать). Появился новый — дописываешь сразу,
> а не «потом».
>
> Задачи текущей подфазы живут не здесь, а в [`impl-phase2-plan.md`](./impl-phase2-plan.md).

---

## Продукт

| Хвост | Подробности | Когда возвращаемся |
|---|---|---|
| **Часть 2 патча свежести** (F8–F16): серверная ревизия секций, `BroadcastChannel` между вкладками, остальные экраны «Акцента». **Решено 01.08.2026: не выпускаем пока** — механизм не додуман, экономит нагрузку, которой нет | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → блок 2.7.1, [ADR-0061](./decisions/0061-cross-device-data-freshness.md) | не раньше дашборда (2.11); вернуться, когда появится понимание, как оно должно работать |
| **Поля без потребителя.** Детектор показывает 4 (`AntiHabitEventView.endedAttemptNumber`/`fromStartedAt`, `AntiHabitView.recordAttemptStartedAt`, `GoalView.pausedAt`), но **фактически их шесть**: `GoalView.completedAt` и `TaskView.completedAt` тихо исчезли из отчёта, когда деталь привычки показала своё `completedAt` — детектор ищет по имени, а не по паре «интерфейс.поле» | `make audit-fields`, разбор — в плане, шаг 2.7.2·7; предел инструмента описан в его шапке | при дашборде: там и решится, что показывать |
| **Связь «Держусь» ↔ «Препятствия» не находится сама.** Селект «Из-за чего» в модалке рецидива виден, только если у человека уже есть свои препятствия; с пустым разделом он даже не узнает, что такая связь есть | обсуждено 31.07.2026; решение — подсказка в модалке + «превратить повторяющийся триггер в препятствие» (не в момент срыва, а когда виден повтор) | 2.11 / 2.8 — там живёт логика «заметил закономерность и подсказал» |
| **Часть 2 геймификации** — XP/очки, уровни, XP-бар и тосты «+N», бонусы за вехи, достижения-счётчики, «Отказался ради важного», анти-чит. Отложено 04.08.2026: число легко становится целью (Гудхарт), а постоянно висящий счётчик не проходит фильтр Принципа 3. Цена возврата названа заранее: **придётся завести `point_events`** с идемпотентностью и revoke | [`gamification-passport.md` §5](./sections/accent/gamification-passport.md) | после MVP, если вообще |
| **Крон вех «держусь»** (`@nestjs/schedule`) — сейчас вехи считаются лениво при чтении, и этого достаточно: push-ов нет, доставка и так «когда человек зашёл» | [`gamification-passport.md` §5](./sections/accent/gamification-passport.md) | вместе с 2.8, где `Recommender` захочет генерить ноты сам (тогда же — лок на случай второго инстанса) |
| **💡 «Жизнь и отзывчивость интерфейса»** (концепт Elmir, 04.08.2026) — сделать отклик системным, а не точечным: мгновенная реакция на действие (оптимистичное обновление вместо ожидания ответа), скелетоны вместо «Загрузка…», появление/исчезновение карточек и строк с переходом, счётчики, «доезжающие» до нового значения, состояния наведения и нажатия у всего кликабельного, переходы между экранами. **Опора уже есть — `[appFocusTarget]`** (2.9·20): один механизм на все экраны, поведение описано правилом, `prefers-reduced-motion` — отдельная ветка, а не «выключить анимацию». Так же собирать и остальное: набор общих директив/утилит, а не анимации по месту, иначе разъедется | концепт — в разговоре 04.08.2026; образец подхода — [`focus-target.directive.ts`](../angular/src/app/shared/ui/focus-target.directive.ts) | после трекерных подфаз; **граница решена — [ADR-0066](./decisions/0066-interface-liveliness-boundaries.md)**: продукт про спокойствие (ADR-0049), поэтому оживляем отклик на действия человека, а не добавляем движение ради движения |

| **Вехи «держусь» не приходят при ручной цели.** Галочка «цель» в форме выключена по умолчанию (тогда лесенка цела), но при включении форма подставляет **30** — и человек не получает ни одной вехи по дороге: `reachedGoals` отбрасывает все ступени ниже `manualTargetDays` (floor). Так задумано — своя цель важнее лесенки, — но по дороге к ней человек не слышит ничего, а лесенка существовала как раз ради поддержки в пути | поймано живой проверкой 04.08.2026 (2.9·15), разбор — `anti-habit-goal-ladder.util.ts` | решить при следующем заходе в «Держусь»: либо ступени ниже своей цели тоже отмечать, либо явно объяснить человеку молчание |

| **Терминология (#1)**: маппинг имён (`/power-ups`→`/micro-wins` и пр.) сквозь доку и роуты, оформить как ADR | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → «Открытые развилки» | при финализации раздела |

| **💡 Концепт «Открыть „Сегодня“ за выбранную дату»** — отметка задним числом (единственное, где гугл-таблица объективно сильнее). Спорный: больше свободы = больше функций = страшнее пользоваться | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → блок 2.7.3, «Концепт на подумать» | не раньше конца фазы 2 |

| **`GET /accent/settings` остался без потребителя** — с 2.11 дашборд берёт состояние паузы из своего снимка. Эндпоинт живой и корректный, но на фронте его никто не зовёт до экрана настроек раздела | поймал `make audit-fields` 03.08.2026; в `ALLOW` с причиной | вместе с экраном `/accent/settings` |

| **Отступ экрана раздела дублируется в каждом компоненте** (`padding: var(--space-4) 0` на корне) — из-за этого он дважды забывался на новых экранах (деталь привычки 2.7.3, дашборд 2.11). Кандидат: перенести один раз в layout `AccentComponent` вокруг `router-outlet` и убрать из семи экранов | замечено Elmir 03.08.2026 | мелкий рефактор, когда будет затишье |

| **💡 Бинарные цели-события не покрыты моделью** (замечание живого пользователя 05.08.2026, прод): четыре рода целей ([ADR-0052](./decisions/0052-accent-goal-direction-and-computed-progress.md)) все про число, а «получить оффер», «переехать», «сдать экзамен» бинарны — либо да, либо нет. Это самый частый тип живых целей, и сейчас человек упирается в выпадающий список, где ни один вариант не подходит. **Три пути:** (1) не добавлять, учить декомпозиции — честно, но человек пришёл записать цель, а не выдумывать ей метрику; (2) пятый род «Событие» с прогрессом 0/100% — просто, но такая цель годами висит на нуле и молча давит; (3) **цель-веха с прогрессом по доле закрытых подцелей** — дерево целей уже есть, измеримость переносится на уровень ниже, где она естественна. **Разбор скриншотов переписки сместил приоритет к подсказке в форме:** человек сам сказал «надо указать размер оффера», то есть за событием стоит число, и его цель закрывается **существующим** родом «Достичь уровня» (доход: сейчас → желаемый). Пятый род «Событие» первоисточник отклонил сам («бинарная цель реально тупая»). Прежде чем проектировать новый род — проверить дешёвую гипотезу: хватает вопроса «ради какого числа это нужно?» | первоисточник, полные цитаты и разбор по объёму доработки — [`user-requests.md` → UR-1](./sections/accent/user-requests.md); решать ADR-ом, а не по ходу | при следующем заходе в «Цели» |

| **💡 Достижимость цели не проверяется** (UR-3, предложение живого пользователя 06.08.2026 — «почитай про SMART»). Разложили модель по SMART: четыре буквы из пяти закрыты давно (`title`+`whyItMatters`, метрика и замеры, `domainKey`/`attributes`, `deadline`), не закрыта одна — **A, достижимость**. Человек может завести «1000 км за месяц», и продукт молча согласится, а через две недели цель станет источником вины. Данных достаточно: из `targetValue`, `startValue` и `deadline` считается требуемый темп, и вместо вопроса «реалистично ли?» продукт может показать следствие — «это 5 км каждый день без выходных». Заодно закрывает UR-1: у «оффера» через доход сразу видно, на сколько в месяц надо вырасти | разбор — [`user-requests.md` → UR-3](./sections/accent/user-requests.md) | при следующем заходе в «Цели», вместе с UR-1 |

| **Ветка отказа по заявке живьём не прогонялась** (E10, 05.08.2026) — одобрение и очередь проверены, отказ нет: «столько знакомых нет, чтобы отказывать». Реш. Elmir: принимаем, всплывёт — поправим. Второй человек при этом не нужен: `telegram_requests_one_pending_per_chat` — **частичный** unique (`where status = 'pending'`), поэтому тот же тестовый чат после закрытия заявки может подать новую, и её отказать. Проверить, что отказ шлёт человеку внятный ответ и заявка уходит из очереди | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → E10; схема — [`telegram-requests.schema.ts:56`](../nest/src/database/schemas/telegram-requests.schema.ts) | пара минут, когда будет настроение; иначе по факту бага |

| **💡 Как связать привычки и цели** — тема, над которой Elmir думает самостоятельно с конца июля 2026 («недели две думаю, пока клода не напрягал»). Живой пользователь дал формулировку, которую стоит взять за основу: **«цель конечна, привычка — компас: есть направление, но нет финиша»**. Прямое следствие: связь **не может быть подчинением** — если привычка станет подзадачей цели, закрытие цели её убьёт, а привычка тем и ценна, что переживает цель. Кандидаты: вклад в метрику (совпали единицы — замеры привычки кормят цель), мотивационная ссылка без арифметики, обратное направление (цель как временное усиление привычки) | постановка и цитаты — [`user-requests.md` → UR-2](./sections/accent/user-requests.md) | **заход Elmir**, не начинать без него |

| **Форму цели человек не бросает, а врёт в поле** — на скриншоте 05.08.2026 видно, что, не найдя подходящего рода для «получить оффер», он выбрал **«Удерживать»** и продолжил. Значит в базе копятся цели с родом, который к ним не относится, а телеметрия покажет обычную созданную цель. Проверить перед доработками формы: сколько таких целей уже есть (род не согласуется с названием/единицей) | контекст — [UR-1](./sections/accent/user-requests.md) | вместе с заходом в «Цели» |

| **Уточнить у Elmir: что значило «привычка кстати не дропнулась»** (реплика 05.08.2026, сразу после переноса его среза с localhost на прод). Два прочтения: (а) серия должна была прерваться за пропущенный день и не прервалась — тогда это **баг расчёта серии**; (б) привычка просто пережила перенос — тогда всё в порядке. Пока не спросим, не понять, заводить ли фикс | реплика в переписке 05.08.2026 | спросить при ближайшем заходе |


| **💡 Восстановление пароля через привязанный Telegram + двусторонняя привязка** (концепт Elmir, 05.08.2026) — как ещё один способ рядом с K из N секретных вопросов. Требует пересмотра статуса привязки: из удобства она становится **фактором доступа**. Тогда же вводится **обмен кодами в обе стороны** (бот просит код с сайта → сайт просит код у бота) и для привязки, и для отвязки: односторонний код доказывает владение обеими сторонами, но не защищает от утечки самого кода (скриншот, шаринг экрана), а обмен требует ещё и доступа к браузеру жертвы. **Открытый вопрос:** человек, потерявший доступ к Telegram, при обмене не отвяжет чат никогда — нужен запасной путь (отвязка по паролю), иначе защита станет ловушкой | привязка сделана в 2.9.1·14 односторонним кодом; действующее восстановление — [ADR-0006](./decisions/0006-registration-field-rules.md) | не раньше, чем закроется 2.9.x; делать одним заходом с восстановлением, оформить как ADR |

| **💡 E2E по жизненному циклу сущностей** (обсуждение Elmir 05.08.2026, делаем **после** 2.9.1) — не покрытие фич, а 5–8 сценариев на раздел «создал → изменил → перенёс → удалил → попробовал вернуть». Повод: баг с осиротевшим переносом жил на стыке двух правильных решений, и юнит-тест на слой его не поймал бы в принципе — каждый слой вёл себя как задумано. Рядом два дешёвых приёма: **чек-лист «что с потомками»** при заведении любой сущности с производными и **инварианты в БД** там, где состояние можно запретить (образец — частичный индекс «одна pending-заявка на чат»). Сотня хрупких тестов хуже восьми надёжных | разбор — в переписке 05.08.2026; фикс, с которого начался разговор, — `f91c05b` | после 2.9.1, удобно вместе с 2.9.2 |

| **Релиз-ноты в нынешнем виде не читаются** — по каждой тонна текста, а в канале они шли бы серией постов. Решено 05.08.2026: жанр меняем (сводка вместо десяти нот, страница вместо простыни, один пост вместо серии), но **сначала исследование вне репозитория** — подбор промптов для лендингов в `~/coding/landing-lab` (папка заведена 05.08.2026) | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → блок 2.9.2 | после деплоя 2.9.1 |



| **Канал может замолчать незаметно.** Доступность Telegram зависит от IP-маршрута РФ-хостинга: соседний сервер того же провайдера до Telegram не достукивается вовсе, а с рабочего замер 05.08.2026 дал **14 успешных запросов из 20**. Лечится повторами (4 попытки, таймаут 8 с), но ошибки доставки глушатся осознанно, поэтому «посты перестали уходить» видно только по каналу или логам — оповещения нет | [ADR-0064](./decisions/0064-telegram-release-channel.md) → «Риск, вскрывшийся при проверке» | подумать о простой проверке «последний релиз объявлен?» при следующем заходе в 2.9.x |

## Эксплуатация

| Хвост | Подробности | Приоритет |
|---|---|---|
| **Бэкап живёт в одном экземпляре и без шифрования.** Решено 01.08.2026: снимаем **вручную по запросу раз в N времени**, автоматизацию и шифрование пока не делаем — осознанный принятый риск | [`backup.md`](./backup.md) | средний (было «высокий»): доступ к серверу закрыт копией кредов, данные людей — по-прежнему в одной копии |
| **Статистика планировщика на проде не обновлялась**: `pg_stat_user_tables` показывает 7 аккаунтов вместо 20 | замечено 31.07.2026; лечится `ANALYZE` | низкий — на таких объёмах планы всё равно через seq scan |
| **Проверять Telegram живьём больше негде.** 10.08.2026 Elmir удалил стейдж-бота `@norms2_stage_bot` и тестовый канал «Нормисы — тест» — вместе со стейджем. Локально проверяется всё, кроме входящего вебхука: Telegram стучится на публичный HTTPS, `localhost` не годится. Следующая работа с ботом упрётся в это: понадобится новый бот у `@BotFather`, новый тестовый канал, новый `secret_token` и `setWebhook`. Либо — проверять на боевом боте, что и есть «дев-прод» | [`deployment.md` §4a](./deployment.md) — раздел оставлен как рецепт | решить до следующего захода в 2.9.x с ботом |
| **Карточка заявки правится только у одного админа** (2.9.3·3а). Доставляется она всем админам с привязанным Telegram, но `telegram_requests.owner_message_id` один — после решения обновится карточка только у того, кому доставили первым; у остальных останется старая, с живыми кнопками. Пока админ один — незаметно; при двух начнёт путать | `telegram.domain-service.ts` → `submitRequest`; схема — `telegram-requests.schema.ts` | когда админов станет больше одного |
| **Срок хранения журнала админа не задан** (2.9.3·6). Таблица `admin_audit_log` только дописывается и ничем не подрезается: на наших объёмах это годы, но «растёт вечно» — не решение, а его отсутствие. Заодно решить, что делать с записями об аккаунтах, которых уже нет (ссылка обнуляется, снимок логина остаётся) | [`api-contracts.md`](./api-contracts.md) → «Журнал действий»; таблица заведена в 2.9.3·6 | ✅ **решено 14.08.2026:** отдельного ретеншена не заводим, инструмент физической зачистки — флаг `paranoid: false` ([ADR-0068](./decisions/0068-deletion-belongs-to-storage.md)). Осталось решить только судьбу записей об удалённых аккаунтах |
| **Роли на фронте живут до перезагрузки вкладки** (найдено 13.08.2026 при ·15). `AuthStore.isAdmin()` считается из аккаунта, полученного при входе и при подъёме страницы, — на каждый переход роли не перезапрашиваются. После снятия роли пункт меню «Админка» и guard раздела остаются до перезагрузки. Данных это не открывает: бэк уже отдаёт 404 (проверено настоящим HTTP), но пункт меню ведёт в «страница не найдена». Чинится перечитыванием `/accounts/me` при входе в раздел | [`admin.guard.ts`](../angular/src/app/features/admin/admin.guard.ts), [`auth-store.service.ts:27`](../angular/src/app/core/auth/auth-store.service.ts) | низкий: снятие роли у нас событие штучное; решить при визуальной приёмке ·15 |
| **Тултип у правого края растягивает страницу по горизонтали** (поймано Elmir 14.08.2026 при приёмке 2.9.3). `.tooltip-host::after` спозиционирован `left: 50%; transform: translateX(-50%)` с `white-space: nowrap`; у кнопки «⋯», прижатой к правому краю карточки, подсказка вылезает за вьюпорт — и, будучи в обычном потоке, добавляет странице горизонтальную прокрутку. Лечится либо вариантом «прижать к правому краю» (`right: 0; transform: none`) для крайних кнопок, либо ограничением ширины с переносом. Не критично, но это ровно тот класс, из-за которого «на узком экране всё едет» | [`_tooltip.scss`](../angular/src/styles/_tooltip.scss); пример — карточка «Держусь» | в верстальный заход (реш. Elmir: 2.10 / конец фазы 2) |
| **Верстку в целом переулучшить** (реш. Elmir 14.08.2026): узкие экраны, переносы, тултипы, переполнения. Копить находки приёмки сюда, а не чинить по одной | [`acceptance-2.9.3.md`](./acceptance-2.9.3.md) — находки приёмки | 2.10 / конец фазы 2 |
| **Снять бан можно только из «моих банов»** — на экране «кто кого позвал» кнопки нет вовсе, хотя с 2.9.3·21 право есть и у предков банившего. То есть механизм работает, а под рукой его нет там, где человек смотрит на свою подветку | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → 2.9.3·23; правило — [ADR-0003, дополнение](./decisions/0003-ban-semantics.md) | мелочь, при следующем заходе в дерево |
| **Токен боевого бота побывал в переписке дважды** (04.08.2026 и 10.08.2026) и **не отозван** — риск принят Elmir осознанно. Значение ушло на серверы модели и осталось в истории чатов, поэтому «доступ к компьютеру только у меня» тут не защита. Цена растёт: бот уже умеет принимать заявки, а с ·3а получит права по роли админа. Ротация — `/revoke` у `@BotFather`, новый токен кладётся в `/home/norms2/.env` **руками, мимо чата** | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → E1 | решить до того, как бот получит право выдавать приглашения |

| **Страница выпуска 2.9.3 из продукта недостижима.** Лендинг существует файлом, а `GET /releases/release-2.9.3` на проде отдаёт **404**: публикация не зарегистрирована, потому что анонс мы сознательно не делали (найдено аудитом ·33 14.08.2026). Перед показом публике надо выбрать осознанно: зарегистрировать публикацию (тогда она появится и в колокольчиках у всех) или оставить лендинг отдельной ссылкой | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → 2.9.3·33 | перед анонсом фаз 1 и 2 |
| **Владение проверено структурно, но не поведенчески** (·32, 14.08.2026). Показано, что 73 из 75 маршрутов с параметром в пути берут аккаунт из Guard и несут его в use-case, а два оставшихся публичны по замыслу. Это доказывает, что владелец *доезжает* до слоя, но не что каждый запрос его *учитывает*: настоящий прогон — подставить чужой `id` живым токеном и увидеть 404 — не делался | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → 2.9.3·32 | до анонса; дешевле всего скриптом на dev с двумя аккаунтами |
| **Уборка заявок идёт только когда бот жив** (·35, 14.08.2026). Протухание и удаление старых гостевых заявок цепляются к входящему апдейту с таймером в памяти процесса — планировщика в проекте нет. У молчащего бота новых заявок и не появляется, поэтому цена мала; но если `@nestjs/schedule` появится по другой причине, перенести уборку туда — одна строка | [`telegram-upkeep.service.ts`](../nest/src/modules/telegram/domain-services/telegram-upkeep.service.ts), [ADR-0064 доп. 14.08.2026](./decisions/0064-telegram-release-channel.md) | низкий |

| **Неочевидные моменты механики** — факты «а почему оно так?», всплывающие в разговоре (лесенка не откручивается назад, ретро-материализация, вечный `pending`…). Копим списком, к концу фазы 2 пройти и решить по каждому | [`impl-phase2-plan.md`](./impl-phase2-plan.md) → «Неочевидные моменты механики» | конец фазы 2 |

## Разработка

| Хвост | Подробности | Приоритет |
|---|---|---|
| ~~**`docker restart` ломает dev-бэкенд**~~ — **закрыто 03.08.2026, дефекта нет.** Проверено живьём: `make dev-restart` (= `down` + `up -d`) пересоздаёт контейнер, кеш `tsconfig.build.tsbuildinfo` и `dist` уходят вместе с ним, бэк поднимается сам. Ломался только **`docker restart`**, который сохраняет файловую систему контейнера вместе с протухшим кешем: инкрементальная сборка рапортует «Found 0 errors» и не эмитит `dist` → `Cannot find module '/app/dist/main'` при живом контейнере. **Правило: перезапуск dev — только через `make`** (`nest/src` монтируется с хоста, остальное живёт в контейнере). Правку в `package.json` не делаем: она заставила бы каждый старт пересобирать всё ради страховки от команды, которой не следует пользоваться | — | закрыто |
| **Правка уже разошедшейся релиз-ноты наружу не едет**: `.md` копируется в `content/`, только если файла там ещё нет | предупреждение записано в [`release-notes.seed.ts`](../nest/src/modules/notifications/seed/release-notes.seed.ts) | не баг (защита живых нот), но помнить |
